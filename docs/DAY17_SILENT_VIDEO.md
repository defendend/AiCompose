# 🎬 День 17: Беззвучное видео (2-3 минуты)

## Формат
- Без озвучки
- Только визуальная демонстрация
- Текстовые заголовки на экране (опционально)
- Фокус на результатах инструментов

---

## 🎥 Сценарий (2-3 минуты)

### Титр 1: "День 17: RAG-запросы" (3 сек)
Статичный экран с текстом

---

### Сцена 1: ask_with_rag (45 сек)

**Титр:** `ask_with_rag` (2 сек)

**Действие:**
1. Открыт чат AiCompose
2. Вводим промпт:
   ```
   Use ask_with_rag to answer: "What is RAG and how does it work?" with top_k=3
   ```
3. Отправляем (Enter)
4. **Ждём ответа** (~8 секунд)
5. **Показываем результат:**
   - Прокрутить вниз, чтобы видеть весь ответ
   - Задержаться на **статистике** (5 сек):
     ```
     📊 Статистика:
       • Найдено фрагментов: 3
       • Источники: rag.md, history.md
       • Средняя релевантность: 0.18
       • Время ответа: 8120ms
       • Токены: 470 → 239
     ```

**План Б (если агент не вызывает инструмент):**
- Переключиться на терминал
- Выполнить curl:
  ```bash
  curl -X POST http://89.169.190.22/api/tools/execute \
    -H 'Content-Type: application/json' \
    -d '{"tool": "ask_with_rag", "arguments": "{\"question\": \"What is RAG?\", \"top_k\": 3}"}'
  ```
- Показать JSON ответ (прокрутить до статистики)

---

### Сцена 2: compare_rag_answers (60 сек)

**Титр:** `compare_rag_answers` (2 сек)

**Действие:**
1. В чате вводим:
   ```
   Use compare_rag_answers to compare: "How does Docker work?" with top_k=2
   ```
2. Отправляем
3. **Ждём ответа** (~12 секунд - два запроса к LLM)
4. **Показываем результат:**
   - Медленно прокрутить весь ответ (20 сек)
   - Показать структуру:
     - `🚫 БЕЗ RAG` (задержка 3 сек)
     - Прокрутить до `✅ С RAG` (задержка 3 сек)
     - Прокрутить до `🔍 Анализ` (задержка 3 сек)
     - Прокрутить до `💡 Вывод` (задержка 3 сек)

**План Б (терминал):**
```bash
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "compare_rag_answers", "arguments": "{\"question\": \"How does Docker work?\", \"top_k\": 2}"}'
```

---

### Сцена 3: Нет данных в RAG (30 сек)

**Действие:**
1. Вводим:
   ```
   Compare RAG answers for: "What is Kubernetes?"
   ```
2. Ждём ответа
3. **Показываем важную часть:**
   - Прокрутить до:
     ```
     ✅ С RAG:
     ...информации о Kubernetes не найдено.
     Найдено фрагментов: 0
     ```
   - Задержка 5 сек
   - Прокрутить до:
     ```
     💡 Вывод:
       RAG не помог - нет релевантной информации в индексе.
     ```
   - Задержка 5 сек

---

### Титр финальный: "День 17 ✓" (3 сек)
Статичный экран

---

## 📋 Технические детали

### Скорость демонстрации
- **Ввод текста:** обычная скорость (или можно вставить Cmd+V)
- **Ожидание ответа:** показать реальное время (8-12 сек)
- **Прокрутка результата:** медленно, чтобы видно было текст (2-3 секунды на секцию)
- **Задержки на важных местах:**
  - Статистика: 5 сек
  - Анализ: 3 сек
  - Выводы: 3 сек

### Что выделить визуально
1. **Статистика в ask_with_rag:**
   - Курсором обвести "Найдено фрагментов: 3"
   - Или просто задержаться на этой секции

2. **Два ответа в compare_rag_answers:**
   - Показать всю структуру: БЕЗ RAG → С RAG → Анализ → Вывод

3. **Честность RAG:**
   - Задержаться на "Найдено фрагментов: 0"

---

## 🎬 Альтернативный сценарий: Только терминал (1.5 мин)

Если хочешь чисто техническое демо без UI:

### 1. ask_with_rag через curl (30 сек)
```bash
# Заголовок в терминале (echo)
echo "=== ask_with_rag: What is RAG? ==="

# Запрос
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "ask_with_rag", "arguments": "{\"question\": \"What is RAG?\", \"top_k\": 3}"}' \
  | python3 -m json.tool

# Прокрутить вывод, показать статистику
```

### 2. compare_rag_answers через curl (45 сек)
```bash
echo "=== compare_rag_answers: How does Docker work? ==="

curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "compare_rag_answers", "arguments": "{\"question\": \"How does Docker work?\"}"}' \
  | python3 -m json.tool

# Медленно прокрутить результат
```

### 3. Нет данных (15 сек)
```bash
echo "=== compare_rag_answers: What is Kubernetes? ==="

curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "compare_rag_answers", "arguments": "{\"question\": \"What is Kubernetes?\"}"}' \
  | python3 -m json.tool
```

---

## 📐 Рекомендации по записи

### Разрешение экрана
- 1920x1080 или выше
- Показывать только окно терминала/чата (без лишнего)
- Увеличить шрифт (16-18pt) для читаемости

### Скорость видео
- Можно ускорить ожидание ответов (2x) в монтаже
- Важные части (статистика, выводы) — оставить нормальную скорость

### Титры (опционально)
Если хочешь добавить текст на экране:
- "ask_with_rag" → автоматический поиск + ответ
- "compare_rag_answers" → сравнение с RAG vs без RAG
- "День 17 ✓"

---

## ⏱️ Хронометраж

| Сцена | Время | Действие |
|-------|-------|----------|
| Титр начало | 3 сек | "День 17: RAG-запросы" |
| ask_with_rag | 45 сек | Промпт + ответ + статистика |
| Титр | 2 сек | "compare_rag_answers" |
| compare_rag_answers | 60 сек | Промпт + 2 ответа + анализ |
| Kubernetes | 30 сек | Демо отсутствия данных |
| Титр конец | 3 сек | "День 17 ✓" |

**Итого: ~2.5 минуты**

---

## ✅ Checklist

- [ ] Шрифт в терминале/чате увеличен (16-18pt)
- [ ] Desktop приложение запущено (или терминал готов)
- [ ] Индекс проиндексирован (9 документов)
- [ ] Оба инструмента проверены (✅ SUCCESS)
- [ ] Экран чист (скрыть dock, меню)
- [ ] Готов Plan Б (curl команды в отдельной вкладке терминала)

---

## 🎯 Главное

**Что должен увидеть зритель:**
1. ✅ ask_with_rag находит 3 фрагмента и даёт ответ со статистикой
2. ✅ compare_rag_answers показывает ДВА ответа с автоматическим анализом
3. ✅ RAG честно признаёт отсутствие данных (0 фрагментов для Kubernetes)

**Без слов понятно:**
- RAG работает
- Есть статистика (источники, релевантность, токены)
- Есть сравнение (с RAG vs без RAG)
- Система честная (не выдумывает)

---

## 💡 Совет

Если хочешь максимально короткое видео (1 минута):
- Пропусти Kubernetes
- Покажи только ask_with_rag (30 сек) и compare_rag_answers (30 сек)
- Титры можно убрать

**Минималистичная версия = максимальная польза!** 🎬
