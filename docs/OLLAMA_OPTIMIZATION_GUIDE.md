# День 28: Оптимизация локальной LLM (Ollama)

Гайд по настройке и оптимизации локальных LLM моделей для AiCompose.

## Содержание

1. [Быстрый старт (Desktop)](#быстрый-старт-desktop)
2. [Установка Ollama](#установка-ollama)
3. [Desktop UI бенчмарк](#desktop-ui-бенчмарк)
4. [CLI бенчмарк](#cli-бенчмарк)
5. [Анализ результатов](#анализ-результатов)
6. [Создание кастомной модели](#создание-кастомной-модели)
7. [Параметры оптимизации](#параметры-оптимизации)
8. [Рекомендации по задачам](#рекомендации-по-задачам)

---

## Быстрый старт (Desktop)

### 1. Установка и запуск Ollama
```bash
# macOS
brew install ollama

# Запуск сервера
ollama serve
```

### 2. Скачивание моделей
```bash
ollama pull qwen2.5:1.5b   # Рекомендуемая (1GB)
ollama pull qwen2.5:0.5b   # Быстрая (400MB)
```

### 3. Запуск приложения
```bash
./gradlew :desktop:run
```

### 4. Открыть бенчмарк
**Меню → Бенчмарк Ollama (🦙)**

### 5. Запустить тест
1. Выбрать модели (чекбоксы)
2. Выбрать тип теста
3. Нажать "Запустить бенчмарк"
4. Смотреть результаты

---

## Установка Ollama

### macOS
```bash
brew install ollama
```

### Linux
```bash
curl -fsSL https://ollama.ai/install.sh | sh
```

### Windows
Скачайте установщик с [ollama.ai](https://ollama.ai)

### Запуск сервера
```bash
# Запуск в фоне
ollama serve &

# Или через brew (macOS)
brew services start ollama
```

### Скачивание моделей
```bash
# Рекомендуемые модели для тестирования
ollama pull qwen2.5:0.5b   # 0.5B параметров, ~400MB
ollama pull qwen2.5:1.5b   # 1.5B параметров, ~1GB
ollama pull llama3.2:3b    # 3B параметров, ~2GB

# Проверка установленных моделей
ollama list
```

---

## Запуск бенчмарка

### Быстрый старт
```bash
cd /path/to/AiCompose
chmod +x scripts/ollama_demo.sh
./scripts/ollama_demo.sh
```

### Что тестирует скрипт

| Тест | Описание | Параметры |
|------|----------|-----------|
| 1. Модели | Сравнение 3 моделей | qwen2.5:0.5b, 1.5b, llama3.2:3b |
| 2. Температура | Влияние на креативность | 0.1, 0.7, 1.2 |
| 3. Длина ответа | Оптимальный max_tokens | 50, 200, 500 |
| 4. Контекст | Размер контекстного окна | 1024, 2048, 4096 |
| 5. Задачи | Разные типы промптов | simple, coding, creative, analytical |
| 6. Квантование | Сравнение q4/q5/q8 | Если доступны |

### Пример вывода
```
╔════════════════════════════════════════════════════════╗
║      🔥 Демо оптимизации локальной LLM (Ollama)          ║
╚════════════════════════════════════════════════════════╝

📋 Тест 1: Сравнение моделей
────────────────────────────
🤖 Модель: qwen2.5:0.5b
  🔧 Базовый
     Модель: qwen2.5:0.5b | t=0.7 | tokens=100 | ctx=2048
     ⏱️  2.00с | 🔤 248 символов (~62 токенов) | ⚡ 31.0 т/с
     📝 AI (Artificial Intelligence) — это область информатики...
```

---

## Анализ результатов

### CSV файл результатов
После запуска создаётся `ollama_benchmark.csv`:

```csv
model,config,temperature,max_tokens,num_ctx,duration_sec,response_length,estimated_tokens,tokens_per_sec
qwen2.5:1.5b,Сбалансированная,0.7,200,2048,1.00,413,103,103.0
qwen2.5:1.5b,Задача_coding,0.7,200,2048,2.00,715,178,89.0
```

### Ключевые метрики

| Метрика | Описание | Хорошее значение |
|---------|----------|------------------|
| `duration_sec` | Время ответа | < 3 сек |
| `tokens_per_sec` | Скорость генерации | > 50 т/с |
| `response_length` | Длина ответа | Зависит от задачи |

### Результаты нашего тестирования

**Топ конфигурации по скорости:**

| # | Модель | Конфигурация | Скорость |
|---|--------|--------------|----------|
| 🥇 | qwen2.5:1.5b | Сбалансированная (t=0.7) | **103 т/с** |
| 🥈 | qwen2.5:1.5b | Креативная (t=1.2) | 93 т/с |
| 🥉 | qwen2.5:1.5b | Задача_coding | 89 т/с |

**Сравнение моделей:**

| Модель | Размер | Скорость | Качество |
|--------|--------|----------|----------|
| qwen2.5:0.5b | 400MB | ⚡⚡⚡ Быстрая | ⭐⭐ Базовое |
| qwen2.5:1.5b | 1GB | ⚡⚡ Средняя | ⭐⭐⭐ Хорошее |
| llama3.2:3b | 2GB | ⚡ Медленная | ⭐⭐⭐⭐ Отличное |

---

## Создание кастомной модели

### Структура Modelfile

```dockerfile
# Modelfile.chatbot - Оптимизированная модель для AiCompose

# Базовая модель
FROM qwen2.5:1.5b

# Параметры генерации
PARAMETER temperature 0.7      # Баланс креативности
PARAMETER num_predict 200      # Максимум токенов в ответе
PARAMETER num_ctx 2048         # Размер контекстного окна
PARAMETER top_k 40             # Top-K sampling
PARAMETER top_p 0.9            # Nucleus sampling
PARAMETER repeat_penalty 1.1   # Штраф за повторения

# Системный промпт
SYSTEM """
Ты — умный ассистент в приложении AiCompose.

🎯 ТВОЯ РОЛЬ:
- Помогаешь с программированием (Kotlin, Compose)
- Отвечаешь кратко и по делу
- Используешь примеры кода

📋 ПРАВИЛА:
1. Отвечай на русском языке
2. Будь конкретным
3. Форматируй код в markdown
"""

# Шаблон (опционально, для совместимости)
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
<|im_start|>assistant
{{ end }}{{ .Response }}"""
```

### Создание и использование

```bash
# Создать кастомную модель
ollama create aicompose-chat -f Modelfile.chatbot

# Проверить
ollama list
# NAME              SIZE
# aicompose-chat    1.0 GB

# Запустить чат
ollama run aicompose-chat

# Или через API
curl http://localhost:11434/api/generate -d '{
  "model": "aicompose-chat",
  "prompt": "Напиши функцию сортировки на Kotlin",
  "stream": false
}'
```

---

## Параметры оптимизации

### Temperature (Температура)

Влияет на "креативность" ответов.

| Значение | Эффект | Использование |
|----------|--------|---------------|
| 0.0-0.3 | Детерминированный | Код, факты, инструкции |
| 0.5-0.7 | Сбалансированный | Общение, объяснения |
| 0.8-1.2 | Креативный | Истории, brainstorming |
| 1.5-2.0 | Хаотичный | Эксперименты |

```bash
# Тест температуры
curl http://localhost:11434/api/generate -d '{
  "model": "qwen2.5:1.5b",
  "prompt": "Напиши хайку про код",
  "options": {"temperature": 1.0}
}'
```

### num_predict (Max Tokens)

Максимальное количество токенов в ответе.

| Значение | Использование |
|----------|---------------|
| 50-100 | Краткие ответы, да/нет |
| 200-300 | Стандартные ответы |
| 500-1000 | Подробные объяснения |
| 2000+ | Длинные статьи, код |

### num_ctx (Context Window)

Размер контекстного окна (история + промпт + ответ).

| Значение | RAM | Использование |
|----------|-----|---------------|
| 1024 | ~2GB | Короткие диалоги |
| 2048 | ~4GB | Стандартные чаты |
| 4096 | ~8GB | Длинные документы |
| 8192 | ~16GB | Анализ кода |

**Важно:** Большой контекст = больше RAM и медленнее.

### Другие параметры

```bash
# Полный список параметров
curl http://localhost:11434/api/generate -d '{
  "model": "qwen2.5:1.5b",
  "prompt": "Hello",
  "options": {
    "temperature": 0.7,
    "num_predict": 200,
    "num_ctx": 2048,
    "top_k": 40,
    "top_p": 0.9,
    "repeat_penalty": 1.1,
    "seed": 42,
    "stop": ["\n\n", "###"]
  }
}'
```

| Параметр | Описание | Default |
|----------|----------|---------|
| `top_k` | Выбор из топ-K токенов | 40 |
| `top_p` | Nucleus sampling | 0.9 |
| `repeat_penalty` | Штраф за повторения | 1.1 |
| `seed` | Для воспроизводимости | random |
| `stop` | Стоп-последовательности | [] |

---

## Рекомендации по задачам

### Для чат-бота (AiCompose)

```json
{
  "model": "qwen2.5:1.5b",
  "temperature": 0.7,
  "num_predict": 200,
  "num_ctx": 2048
}
```

**Почему:**
- 1.5B — баланс скорости и качества
- t=0.7 — естественные ответы
- 200 токенов — достаточно для большинства ответов

### Для генерации кода

```json
{
  "model": "llama3.2:3b",
  "temperature": 0.3,
  "num_predict": 500,
  "num_ctx": 4096
}
```

**Почему:**
- 3B — лучше понимает код
- t=0.3 — меньше ошибок
- 500 токенов — полноценные функции
- 4096 контекст — для анализа файлов

### Для быстрых ответов

```json
{
  "model": "qwen2.5:0.5b",
  "temperature": 0.1,
  "num_predict": 100,
  "num_ctx": 1024
}
```

**Почему:**
- 0.5B — минимальная модель
- t=0.1 — стабильные ответы
- 100 токенов — краткость
- 1024 контекст — минимум RAM

### Для креативных задач

```json
{
  "model": "llama3.2:3b",
  "temperature": 1.0,
  "num_predict": 1000,
  "num_ctx": 2048,
  "top_p": 0.95
}
```

---

## Квантование моделей

### Что такое квантование

Сжатие весов модели для уменьшения размера и ускорения.

| Формат | Размер | Качество | Скорость |
|--------|--------|----------|----------|
| FP16 | 100% | ⭐⭐⭐⭐⭐ | ⚡ |
| Q8_0 | 50% | ⭐⭐⭐⭐ | ⚡⚡ |
| Q5_0 | 35% | ⭐⭐⭐ | ⚡⚡⚡ |
| Q4_0 | 25% | ⭐⭐ | ⚡⚡⚡⚡ |

### Скачивание квантованных моделей

```bash
# Разные квантования llama3.2:3b
ollama pull llama3.2:3b-q4_0   # Самый быстрый
ollama pull llama3.2:3b-q5_0   # Баланс
ollama pull llama3.2:3b-q8_0   # Лучшее качество

# Сравнение размеров
ollama list
# llama3.2:3b-q4_0    1.2 GB
# llama3.2:3b-q5_0    1.5 GB
# llama3.2:3b-q8_0    2.0 GB
# llama3.2:3b         2.0 GB (FP16)
```

---

## Интеграция с AiCompose

### Добавление Ollama как провайдера (будущее)

```kotlin
// LLMClient.kt
interface LLMClient {
    suspend fun complete(request: LLMRequest): LLMResponse
}

// OllamaClient.kt
class OllamaClient(
    private val baseUrl: String = "http://localhost:11434"
) : LLMClient {

    override suspend fun complete(request: LLMRequest): LLMResponse {
        val response = httpClient.post("$baseUrl/api/generate") {
            contentType(ContentType.Application.Json)
            setBody(OllamaRequest(
                model = "aicompose-chat",
                prompt = request.messages.last().content,
                options = OllamaOptions(
                    temperature = request.temperature ?: 0.7f,
                    num_predict = request.maxTokens ?: 200
                )
            ))
        }
        return response.body()
    }
}
```

### Переключение провайдеров

```kotlin
// AppModule.kt
single<LLMClient> {
    when (System.getenv("LLM_PROVIDER")) {
        "ollama" -> OllamaClient()
        else -> DeepSeekClient()
    }
}
```

---

## Troubleshooting

### Ollama не запускается

```bash
# Проверить статус
curl http://localhost:11434/api/tags

# Если ошибка, перезапустить
pkill ollama
ollama serve
```

### Модель не найдена

```bash
# Проверить установленные модели
ollama list

# Скачать нужную
ollama pull qwen2.5:1.5b
```

### Медленная генерация

1. Уменьшить `num_ctx` (2048 → 1024)
2. Использовать квантованную модель (q4_0)
3. Уменьшить `num_predict`
4. Закрыть другие приложения

### Ошибка памяти

```bash
# Проверить использование RAM
htop

# Использовать меньшую модель
ollama run qwen2.5:0.5b
```

---

## Полезные команды

```bash
# Информация о модели
ollama show qwen2.5:1.5b

# Удаление модели
ollama rm qwen2.5:0.5b

# Копирование модели
ollama cp qwen2.5:1.5b my-model

# Список запущенных моделей
ollama ps

# Остановка модели
ollama stop qwen2.5:1.5b
```

---

## Ссылки

- [Ollama Documentation](https://github.com/ollama/ollama/blob/main/docs/README.md)
- [Modelfile Reference](https://github.com/ollama/ollama/blob/main/docs/modelfile.md)
- [API Reference](https://github.com/ollama/ollama/blob/main/docs/api.md)
- [Model Library](https://ollama.ai/library)
