# День 27: Видео-демо VPS Local LLM (без слов)

## Подготовка

### Терминал 1: SSH к серверу
```bash
ssh defendend@89.169.190.22
```

### Терминал 2: Desktop приложение
```bash
cd /Users/defendend/horobot/AiCompose
./gradlew :desktop:run
```

---

## Сценарий демо (3-4 минуты)

### Сцена 1: Показ сервера (30 сек)
```
[SSH терминал]
↓
$ ssh defendend@89.169.190.22
↓
$ ollama list
NAME            ID              SIZE      MODIFIED
qwen2.5:0.5b    a8b0c5157701    397 MB    ...
↓
$ systemctl status ollama
● ollama.service - Ollama Service
   Active: active (running)
↓
$ systemctl status aicompose
● aicompose.service - AiCompose Backend
   Active: active (running)
```

**Фокус камеры:** Терминал с командами

---

### Сцена 2: Показ конфигурации (20 сек)
```
[SSH терминал]
↓
$ sudo cat /opt/aicompose/.env
↓
LLM_PROVIDER=ollama
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:0.5b
```

**Фокус камеры:** Файл конфигурации

---

### Сцена 3: Логи сервера (20 сек)
```
[SSH терминал]
↓
$ sudo journalctl -u aicompose -n 10
↓
INFO [Koin] - 🤖 Using Ollama LLM: http://localhost:11434 (model: qwen2.5:0.5b)
INFO [Koin] - Using PostgreSQL conversation repository
```

**Фокус камеры:** Строка "Using Ollama LLM"

---

### Сцена 4: Тест через curl (30 сек)
```
[SSH терминал]
↓
$ curl -X POST http://localhost:8080/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "Hello"}'
↓
{
  "message": {
    "content": "Hello! How can I help you today?",
    "tokenUsage": {...}
  }
}
```

**Фокус камеры:** Запрос → Ответ

---

### Сцена 5: Запуск Desktop приложения (20 сек)
```
[Локальный терминал]
↓
$ ./gradlew :desktop:run
↓
[Ждём запуска приложения]
↓
[Показать окно приложения]
```

**Фокус камеры:** Терминал → Окно приложения

---

### Сцена 6: Чат с локальной моделью (60 сек)
```
[Desktop приложение]
↓
[Ввести сообщение]
"Привет! Расскажи о себе"
↓
[Отправить → показать streaming ответ]
↓
[Показать что ответ приходит от локальной модели]
↓
[Ввести второе сообщение]
"Что ты умеешь?"
↓
[Показать что контекст сохраняется]
```

**Фокус камеры:** Поле ввода → Streaming ответ → История

---

### Сцена 7: Показ токенов и статистики (20 сек)
```
[Desktop приложение]
↓
[Навести на статистику токенов в сообщении]
🔢 949 (939→10)  💰 $0.0001
↓
[Показать что токены считаются]
```

**Фокус камеры:** Статистика токенов

---

### Сцена 8: Мониторинг на сервере (30 сек)
```
[SSH терминал - split screen]
↓
$ sudo journalctl -u aicompose -f
↓
[Показать логи в реальном времени]
[LLM_REQUEST] Model: qwen2.5:0.5b, Messages: 2
[LLM_RESPONSE] Duration: 1234ms
```

**Фокус камеры:** Split screen — приложение + логи

---

### Сцена 9: Финал (10 сек)
```
[Показать архитектуру]
Desktop → VPS Backend → Ollama (local)
         ↓
    [Никаких внешних API]
```

---

## Ключевые кадры для скриншотов

1. **ollama list** — список моделей на сервере
2. **Конфигурация .env** — LLM_PROVIDER=ollama
3. **Логи сервера** — "Using Ollama LLM"
4. **curl тест** — запрос и ответ
5. **Desktop чат** — streaming ответ от локальной модели
6. **Токены** — статистика использования

---

## Текст для субтитров (опционально)

| Время | Текст |
|-------|-------|
| 0:00 | Ollama установлен на VPS сервере |
| 0:30 | Конфигурация: LLM_PROVIDER=ollama |
| 0:50 | Backend использует локальную модель |
| 1:20 | Тест через curl |
| 1:50 | Запуск Desktop приложения |
| 2:10 | Чат работает через локальную LLM |
| 3:10 | Статистика токенов |
| 3:30 | Мониторинг в реальном времени |

---

## Чек-лист перед записью

- [ ] Ollama запущен на VPS (`systemctl status ollama`)
- [ ] Модель скачана (`ollama list`)
- [ ] Backend настроен на Ollama (`LLM_PROVIDER=ollama`)
- [ ] Backend работает (`systemctl status aicompose`)
- [ ] Логи показывают "Using Ollama LLM"
- [ ] Desktop приложение запускается
- [ ] Разрешение экрана подходит для записи
- [ ] Split screen настроен (терминал + приложение)

---

## Альтернативный сценарий: Сравнение провайдеров

### Если нужно показать переключение:
```
[SSH терминал]
↓
$ sudo nano /opt/aicompose/.env
# Изменить LLM_PROVIDER=deepseek
↓
$ sudo systemctl restart aicompose
↓
[Показать логи]
INFO [Koin] - 🌐 Using DeepSeek LLM API
↓
[Отправить сообщение — показать разницу в скорости/качестве]
↓
$ sudo nano /opt/aicompose/.env
# Вернуть LLM_PROVIDER=ollama
↓
$ sudo systemctl restart aicompose
```

---

## Команды для копирования

```bash
# SSH
ssh defendend@89.169.190.22

# Проверка Ollama
ollama list
systemctl status ollama

# Проверка Backend
systemctl status aicompose
sudo journalctl -u aicompose -n 20

# Конфигурация
sudo cat /opt/aicompose/.env

# Тест API
curl -X POST http://localhost:8080/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "Hello"}'

# Live логи
sudo journalctl -u aicompose -f
```
