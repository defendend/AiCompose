# 📚 RAG (Retrieval-Augmented Generation) Demo

## Описание

Система векторного поиска по документам с эмбеддингами. Агент может:
- Индексировать документы (README, код, статьи)
- Разбивать на чанки с перекрытием
- Генерировать TF-IDF эмбеддинги
- Выполнять семантический поиск

## Возможности

### 1. Индексация документов
- Поддержка форматов: `.md`, `.txt`, `.kt`, `.java`, `.js`, `.py`, `.ts`
- Автоматическая разбивка на чанки по 500 символов
- Перекрытие 50 символов между чанками
- Генерация векторных эмбеддингов (TF-IDF)
- Сохранение в JSON индекс

### 2. Векторный поиск
- Семантический поиск по содержанию
- Косинусное сходство векторов
- Ранжирование результатов по релевантности
- Топ-K наиболее похожих фрагментов

### 3. Простота использования
- Не требует внешних API (работает локально)
- Нет зависимостей от ML библиотек
- Легковесный TF-IDF векторайзер
- Сохранение/загрузка индекса в JSON

## Инструменты

| Инструмент | Описание |
|------------|----------|
| `rag_index_documents` | Индексирует документы из файла или директории |
| `rag_search` | Выполняет семантический поиск по индексу |
| `rag_index_info` | Показывает информацию об индексе |

## Примеры использования

### Пример 1: Индексация документации проекта

**Промпт:**
```
Проиндексируй документацию из директории docs
```

**Что делает агент:**
1. Вызывает `rag_index_documents` с параметром `path: "docs"`
2. Сканирует все `.md` и `.txt` файлы
3. Разбивает на чанки
4. Генерирует эмбеддинги
5. Сохраняет индекс в `document_index.json`

**Результат:**
```
📚 Индексация завершена

Обработано:
- Путь: docs
- Файлов: 12
- Чанков: 85
- Индекс: /opt/aicompose/document_index.json

Примеры источников:
  • ARCHITECTURE.md
  • API_GUIDE.md
  • DOCKER_DEMO.md
  • PIPELINE_DEMO.md
  • README.md
```

---

### Пример 2: Поиск по индексу

**Промпт:**
```
Найди информацию про Docker инструменты в проиндексированных документах
```

**Что делает агент:**
1. Вызывает `rag_search` с query: "Docker инструменты"
2. Генерирует вектор для запроса
3. Вычисляет сходство со всеми чанками
4. Возвращает топ-3 наиболее релевантных

**Результат:**
```
🔍 Поиск по запросу: "Docker инструменты"

Найдено 3 релевантных фрагментов:

--- Результат 1 (релевантность: 0.89) ---
Источник: DOCKER_DEMO.md

# Docker Environment Integration

Агент может управлять Docker контейнерами на сервере:
- docker_run - запуск контейнеров
- docker_exec - выполнение команд
- docker_logs - просмотр логов
...

--- Результат 2 (релевантность: 0.76) ---
Источник: README.md

## Docker инструменты

Управление контейнерами:
- Запуск nginx, postgres, redis
- Проверка работы сервисов
...
```

---

### Пример 3: Индексация исходного кода

**Промпт:**
```
Проиндексируй весь Kotlin код из backend/src/main/kotlin
```

**Что делает агент:**
1. Сканирует директорию рекурсивно
2. Находит все `.kt` файлы
3. Разбивает код на чанки
4. Создает индекс

**Результат:**
Индекс из ~200 чанков кода

**Затем можно искать:**
```
Найди код который работает с Docker

Найди реализацию RAG системы

Где используется ToolRegistry
```

---

### Пример 4: Проверка индекса

**Промпт:**
```
Покажи информацию о текущем индексе документов
```

**Результат:**
```
📊 Информация об индексе

Статус: Активен ✅
Документов: 245
Файл индекса: /opt/aicompose/document_index.json
Размер файла: 1523 KB
Последнее обновление: 2025-12-21T19:45:00Z
```

---

## API примеры

### Индексация через API

```bash
curl -X POST http://89.169.190.22/api/chat \
  -H 'Content-Type: application/json' \
  -d '{
    "message": "Index all markdown files from docs directory"
  }'
```

### Поиск через API

```bash
curl -X POST http://89.169.190.22/api/chat \
  -H 'Content-Type: application/json' \
  -d '{
    "message": "Find information about Pipeline tools in indexed documents"
  }'
```

---

## Архитектура

### Компоненты

```
DocumentChunker
├── loadDocument() - загрузка файла
├── chunkText() - разбивка на чанки
├── chunkFile() - обработка файла
└── chunkDirectory() - рекурсивное сканирование

SimpleEmbeddings (TF-IDF)
├── fit() - обучение на корпусе
├── embed() - генерация вектора
├── tokenize() - токенизация
├── normalize() - нормализация вектора
└── cosineSimilarity() - вычисление сходства

DocumentIndex
├── indexChunks() - индексация чанков
├── search() - поиск по запросу
├── save() - сохранение в JSON
└── load() - загрузка из JSON
```

### Параметры чанкинга

```kotlin
ChunkConfig(
    chunkSize = 500,        // Размер чанка в символах
    chunkOverlap = 50,      // Перекрытие между чанками
    minChunkSize = 100      // Минимальный размер
)
```

### Формат индекса (JSON)

```json
{
  "entries": [
    {
      "id": "README.md_chunk_0",
      "source": "README.md",
      "content": "# AiCompose\n\n...",
      "embedding": [0.23, 0.15, 0.08, ...]
    }
  ],
  "vectorDimension": 5000,
  "totalDocuments": 245,
  "createdAt": 1734812700000
}
```

---

## Ограничения

- **Размер словаря**: 5000 терминов (топ по частоте)
- **Алгоритм**: TF-IDF (простой, но эффективный)
- **Язык**: Русский + английский
- **Хранилище**: JSON файл (для больших объемов лучше использовать FAISS)

---

## Улучшения для продакшена

### 1. FAISS для больших объемов
```kotlin
// Вместо простого поиска по всем векторам
// использовать FAISS IndexFlatIP
```

### 2. OpenAI/Cohere эмбеддинги
```kotlin
// Вместо TF-IDF использовать
// text-embedding-3-small или embed-multilingual-v3
```

### 3. PostgreSQL с pgvector
```sql
CREATE EXTENSION vector;
CREATE TABLE documents (
  id SERIAL PRIMARY KEY,
  content TEXT,
  embedding vector(1536)
);
```

### 4. Chunking стратегии
- Semantic chunking (по смыслу)
- Sentence-window (окна предложений)
- Recursive character splitting

---

## Тестирование

### Локально

```bash
# 1. Создать тестовые документы
mkdir test_docs
echo "Docker позволяет запускать контейнеры" > test_docs/docker.md
echo "Kotlin - современный язык JVM" > test_docs/kotlin.md

# 2. Запустить backend
./gradlew :backend:run

# 3. В Desktop чате:
"Проиндексируй test_docs"
"Найди про Docker"
```

### На сервере

Индекс сохраняется в `/opt/aicompose/document_index.json` и переживает перезапуск сервера.

---

## Результат

✅ **Локальный индекс документов с эмбеддингами**
✅ **Семантический поиск без внешних API**
✅ **Работает с кодом, markdown, текстом**
✅ **Сохранение/загрузка индекса в JSON**
✅ **Интеграция с AI агентом**

Агент теперь может находить информацию в документации и коде проекта! 🔍📚
