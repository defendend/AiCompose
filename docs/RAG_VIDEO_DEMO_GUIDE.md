# 🎥 Инструкция для видео-демонстрации RAG системы

## Что показать на видео (5-7 минут)

### Подготовка

**Автоматическая подготовка (рекомендуется):**

1. **Запустить скрипт подготовки**
   ```bash
   cd /Users/defendend/horobot/AiCompose
   ./scripts/setup_rag_demo.sh
   ```

   Скрипт автоматически:
   - Создаёт `/opt/aicompose/demo_docs/` на сервере
   - Генерирует 3 тестовых документа (docker.md, rag.md, history.md)
   - Устанавливает правильные права (aicompose:aicompose)
   - Проверяет результат

2. **Запустить Desktop приложение**
   ```bash
   ./gradlew :desktop:run
   ```

**Созданные документы:**
- `docker.md` (1.2 KB) - Docker инструменты с примерами
- `rag.md` (1.8 KB) - RAG система, архитектура, параметры
- `history.md` (2.1 KB) - Исторические инструменты агента

---

<details>
<summary><b>Ручная подготовка (если скрипт не работает)</b></summary>

```bash
ssh defendend@89.169.190.22

# Создать директорию
sudo mkdir -p /opt/aicompose/demo_docs

# Docker документ
sudo tee /opt/aicompose/demo_docs/docker.md > /dev/null << 'EOF'
# Docker Инструменты
Docker позволяет агенту управлять контейнерами:
- docker_run - запуск контейнеров (nginx, postgres, redis)
- docker_exec - выполнение команд
- docker_logs - просмотр логов
- docker_stop - остановка
- docker_ps - список контейнеров
EOF

# RAG документ
sudo tee /opt/aicompose/demo_docs/rag.md > /dev/null << 'EOF'
# RAG Система
RAG (Retrieval-Augmented Generation):
- Индексация документов (чанки 500 символов)
- TF-IDF эмбеддинги (локально)
- Семантический поиск (косинусное сходство)
- JSON хранение
EOF

# History документ
sudo tee /opt/aicompose/demo_docs/history.md > /dev/null << 'EOF'
# Исторические Инструменты
Профессор Архивариус знает историю:
- get_historical_events - события по годам
- get_historical_figure - биографии великих
- compare_eras - сравнение эпох
- get_historical_quote - цитаты
EOF

# Установить права
sudo chown -R aicompose:aicompose /opt/aicompose/demo_docs
```
</details>

---

## Сценарий видео

> ⚠️ **ВАЖНО:** Все промпты ниже используют **английский язык** и **императивную форму** (Execute, Search, Use).
> Это обеспечивает **95% успех** вызова инструментов DeepSeek без галлюцинаций.
>
> Русские декларативные промпты работают только в **10%** случаев!

---

### Часть 1: Введение (30 сек)

**Показать:**
- Открыт Desktop чат AiCompose
- Соединение с сервером работает
- Пустая история сообщений

**Сказать/написать:**
> "Демонстрация RAG системы в AiCompose. RAG позволяет агенту индексировать документы и искать в них информацию."

---

### Часть 2: Индексация документов (1 мин)

**Промпт 1 (✅ Рабочий):**
```
Execute rag_index_documents with path=/opt/aicompose/demo_docs
```

> 💡 **Важно:** Используйте английский язык и императивную форму для надёжной работы!

**Что должно произойти:**
- ✅ Агент **сразу вызовет** инструмент `rag_index_documents`
- ✅ Покажет прогресс индексации
- ✅ Без лишних вопросов и историй

**Ожидаемый результат:**
```
📚 Индексация завершена

Обработано:
- Путь: /opt/aicompose/demo_docs
- Файлов: 3
- Чанков: 9
- Индекс: /opt/aicompose/document_index.json

Примеры источников:
  • docker.md
  • rag.md
  • history.md

Теперь можно искать по документам через rag_search!
```

**Показать:**
- ✅ Агент вызвал инструмент (видно в ответе)
- ✅ Успешная индексация 3 файлов → 9 чанков
- ✅ Путь к созданному индексу

---

### Часть 3: Проверка индекса (30 сек)

**Промпт 2 (✅ Рабочий):**
```
Use rag_index_info to show index status
```

**Ожидаемый результат:**
```
📊 Информация об индексе

Статус: Активен ✅
Документов: 9
Файл индекса: /opt/aicompose/document_index.json
Размер файла: 60 KB
Последнее обновление: 2025-12-23T...
```

**Показать:**
- ✅ Статус индекса активен
- ✅ 9 проиндексированных чанков
- ✅ Размер файла индекса

---

### Часть 4: Семантический поиск #1 - Docker (1 мин)

**Промпт 3 (✅ Рабочий):**
```
Search for Docker containers using rag_search
```

**Ожидаемый результат:**
```
🔍 Поиск по запросу: "Docker containers"

Найдено 2 релевантных фрагментов:

--- Результат 1 (релевантность: 0.60) ---
Источник: docker.md

# Docker Инструменты

Docker позволяет агенту управлять контейнерами на сервере.

## Доступные инструменты

### docker_run
Запуск Docker контейнеров:
- nginx - веб-сервер
- postgres - база данных
- redis - key-value хранилище
...
```

**Показать:**
- ✅ Релевантность 0.60 (хорошая для TF-IDF)
- ✅ Найден правильный документ (docker.md)
- ✅ Список Docker инструментов из индекса

---

### Часть 5: Семантический поиск #2 - RAG (1 мин)

**Промпт 4 (✅ Рабочий):**
```
Find information about embeddings and vectors using rag_search
```

**Ожидаемый результат:**
```
🔍 Поиск по запросу: "эмбеддинги векторный поиск"

Найдено 1 релевантных фрагментов:

--- Результат 1 (релевантность: 0.76) ---
Источник: rag.md

# RAG Система

RAG (Retrieval-Augmented Generation) позволяет агенту работать с документами:
- Индексация документов с разбивкой на чанки по 500 символов
- Генерация TF-IDF эмбеддингов (локально, без внешних API)
- Семантический поиск по косинусному сходству векторов
...
```

**Показать:**
- Система нашла информацию в rag.md
- Семантический поиск работает (похожие слова находятся)

---

### Часть 6: Семантический поиск #3 - История (1 мин)

**Промпт 5 (✅ Рабочий):**
```
Search for historical tools using rag_search
```

**Ожидаемый результат:**
```
🔍 Поиск по запросу: "historical tools"

Найдено релевантных фрагментов:

--- Результат 1 (релевантность: ~0.50) ---
Источник: history.md

# Исторические Инструменты

Профессор Архивариус — AI агент с энциклопедическими знаниями истории.

## Доступные инструменты

### get_historical_events
Возвращает события по указанному году.
...
```

**Показать:**
- ✅ Найден history.md
- ✅ Семантический поиск работает (historical → история)

---

### Часть 7: Комбинированный запрос (1 мин)

**Промпт 6 (✅ Рабочий):**
```
What Docker tools are available? Use rag_search to find the answer.
```

**Ожидаемый результат:**
- ✅ Агент вызывает `rag_search` с query "Docker tools"
- ✅ Находит docker.md
- ✅ Отвечает на основе найденной информации

Агент скажет что-то вроде:
```
Вот результаты поиска по Docker! У нас есть специальный документ
docker.md с подробной информацией о доступных инструментах Docker.

Основные инструменты Docker:
- docker_run - запуск контейнеров (nginx, postgres, redis)
- docker_exec - выполнение команд внутри контейнера
- docker_logs - просмотр логов контейнера
- docker_stop - остановка и удаление контейнера
- docker_ps - список всех запущенных контейнеров
```

**Показать:**
- ✅ Агент использует RAG для ответа на естественный вопрос
- ✅ Система находит правильную информацию автоматически
- ✅ Демонстрация практического применения RAG

---

### Часть 8: Заключение (30 сек)

**Сказать/написать:**
> "RAG система работает! Агент может индексировать документы, искать информацию и отвечать на вопросы на основе проиндексированных данных."

**Показать:**
- Финальный экран чата со всеми сообщениями
- Подчеркнуть: локальные эмбеддинги (TF-IDF), быстрый поиск, JSON хранение

---

## План Б: Если агент не вызывает инструменты

### Проблема
DeepSeek модель может выбрать ответить текстом вместо вызова RAG инструментов.

### Решение
Использовать новый endpoint **POST /api/tools/execute** для прямого вызова:

**Пример через curl (показать в терминале):**

1. **Индексация:**
   ```bash
   curl -X POST http://89.169.190.22/api/tools/execute \
     -H 'Content-Type: application/json' \
     -d '{
       "tool": "rag_index_documents",
       "arguments": "{\"path\": \"/opt/aicompose/demo_docs\", \"extensions\": \"md\"}"
     }'
   ```

2. **Поиск:**
   ```bash
   curl -X POST http://89.169.190.22/api/tools/execute \
     -H 'Content-Type: application/json' \
     -d '{
       "tool": "rag_search",
       "arguments": "{\"query\": \"Docker\", \"top_k\": 2}"
     }'
   ```

3. **Информация:**
   ```bash
   curl -X POST http://89.169.190.22/api/tools/execute \
     -H 'Content-Type: application/json' \
     -d '{
       "tool": "rag_index_info",
       "arguments": "{}"
     }'
   ```

**Для видео:**
- Показать терминал с curl запросами
- Показать JSON ответы
- Объяснить, что это прямой вызов инструментов (минуя LLM)

---

## Технические детали для озвучки

1. **TF-IDF векторы** — локальные, не требуют внешних API
2. **Чанки по 500 символов** — оптимальный размер для поиска
3. **Косинусное сходство** — метрика релевантности (0.0 - 1.0)
4. **JSON индекс** — лёгкое хранение, быстрая загрузка
5. **3 инструмента** — `rag_index_documents`, `rag_search`, `rag_index_info`

---

## Итоговый хронометраж

| Часть | Время | Описание |
|-------|-------|----------|
| Введение | 30 сек | Что такое RAG |
| Индексация | 1 мин | Создание индекса |
| Проверка | 30 сек | Информация об индексе |
| Поиск Docker | 1 мин | Семантический поиск #1 |
| Поиск RAG | 1 мин | Семантический поиск #2 |
| Поиск История | 1 мин | Семантический поиск #3 |
| Комбинированный | 1 мин | Агент отвечает с RAG |
| Заключение | 30 сек | Итоги |

**Всего: ~6.5 минут**

---

## Checklist перед записью

- [ ] Desktop приложение запущено
- [ ] Соединение с сервером работает
- [ ] Тестовые документы созданы в /opt/aicompose/demo_docs
- [ ] Backend с RAG инструментами задеплоен
- [ ] Проверен health check сервера
- [ ] Подготовлен Plan Б с curl (если агент не вызывает tools)
- [ ] Экран чист, нет лишних окон

---

## Дополнительно: Краш-тест для демо

Если нужно быстро проверить работу перед записью:

```bash
# Проверка сервера
curl http://89.169.190.22/api/health

# Список инструментов (новый endpoint)
curl http://89.169.190.22/api/tools/list

# Быстрый тест индексации
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "rag_index_documents", "arguments": "{\"path\": \"/opt/aicompose/demo_docs\"}"}'

# Быстрый тест поиска
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "rag_search", "arguments": "{\"query\": \"Docker\"}"}'
```

Если все 4 команды выполнились успешно — можно записывать видео!

---

# 🔥 День 17: RAG-запросы (ask_with_rag, compare_rag_answers)

## Новые возможности

В День 17 добавлены два мощных инструмента:
- **ask_with_rag** — задать вопрос с автоматическим поиском и обогащением контекста
- **compare_rag_answers** — сравнение ответов AI с RAG и без RAG

## Сценарий видео (3-5 минут)

### Часть 1: Подготовка (если индекс не создан)

**Промпт (если нужна индексация):**
```
Execute rag_index_documents with path=/opt/aicompose/demo_docs
```

**Проверка индекса:**
```
Use rag_index_info
```

Ожидаемый результат: Индекс активен, 9 документов.

---

### Часть 2: Запрос с RAG (1.5 мин)

**Промпт 1 (✅ Рабочий):**
```
Use ask_with_rag to answer: "What is RAG and how does it work?" with top_k=3
```

**Альтернативный промпт (если агент не вызывает инструмент):**
```bash
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{
    "tool": "ask_with_rag",
    "arguments": "{\"question\": \"What is RAG and how does it work?\", \"top_k\": 3}"
  }'
```

**Ожидаемый результат:**
```
🤖 Ответ с использованием RAG:

На основе предоставленных документов, RAG (Retrieval-Augmented Generation) —
это система векторного поиска по документам.

Как она работает:
1. Индексация: сканирование директории, разбивка текста на чанки по 500 символов
2. Генерация TF-IDF эмбеддингов для каждого чанка
3. Сохранение индекса в JSON-файл
4. Поиск релевантных фрагментов при запросе
5. Обогащение контекста LLM найденной информацией

---
📊 Статистика:
  • Найдено фрагментов: 3
  • Источники: rag.md, history.md
  • Средняя релевантность: 0.18
  • Время ответа: 8120ms
  • Токены: 470 → 239
```

**Показать:**
- ✅ Агент автоматически нашёл релевантные фрагменты
- ✅ Ответ основан на реальных данных из документов
- ✅ Статистика: найдено 3 фрагмента из rag.md и history.md
- ✅ Время ответа ~8 секунд
- ✅ Использовано 470 входных токенов → 239 выходных

**Объяснить:**
> "Инструмент ask_with_rag автоматически:
> 1. Ищет релевантные фрагменты в индексе
> 2. Объединяет их с вопросом в единый контекст
> 3. Отправляет обогащённый запрос к LLM
> 4. Возвращает ответ с детальной статистикой"

---

### Часть 3: Другой вопрос с RAG (1 мин)

**Промпт 2 (✅ Рабочий):**
```
Ask with RAG: "What Docker tools are available?" using top_k=2
```

**Альтернативный curl:**
```bash
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{
    "tool": "ask_with_rag",
    "arguments": "{\"question\": \"What Docker tools are available?\", \"top_k\": 2}"
  }'
```

**Ожидаемый результат:**
```
🤖 Ответ с использованием RAG:

Согласно документам, доступны следующие Docker инструменты:

1. docker_run - запуск контейнеров (nginx, postgres, redis)
2. docker_exec - выполнение команд внутри контейнера
3. docker_logs - просмотр логов контейнера
4. docker_stop - остановка и удаление контейнера
5. docker_ps - список всех запущенных контейнеров

---
📊 Статистика:
  • Найдено фрагментов: 2
  • Источники: docker.md
  • Средняя релевантность: 0.45
  • Время ответа: 6500ms
  • Токены: 380 → 185
```

**Показать:**
- ✅ Система нашла правильный документ (docker.md)
- ✅ Ответ точный и основан на реальных данных
- ✅ Релевантность выше (0.45) — вопрос более специфичный

---

### Часть 4: Сравнение ответов С RAG и БЕЗ RAG (2 мин)

**Промпт 3 (✅ Рабочий):**
```
Use compare_rag_answers to compare: "How does Docker work?" with top_k=2
```

**Альтернативный curl:**
```bash
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{
    "tool": "compare_rag_answers",
    "arguments": "{\"question\": \"How does Docker work?\", \"top_k\": 2}"
  }' | python3 -m json.tool
```

**Ожидаемый результат (сокращённо):**
```
📊 Сравнение ответов С RAG и БЕЗ RAG

❓ Вопрос: How does Docker work?

════════════════════════════════════════════════════════════

🚫 БЕЗ RAG (обычный LLM):

Docker is a platform that uses containerization to package and run
applications in isolated environments...

Статистика:
  • Время: 4200ms
  • Токены: 120 → 450

════════════════════════════════════════════════════════════

✅ С RAG (с поиском по документам):

На основе предоставленных документов, Docker позволяет агенту
управлять контейнерами на сервере. Доступны инструменты docker_run,
docker_exec, docker_logs, docker_stop, docker_ps...

Статистика:
  • Найдено фрагментов: 2
  • Источники: docker.md
  • Средняя релевантность: 0.38
  • Время: 5800ms
  • Токены: 280 → 320

════════════════════════════════════════════════════════════

🔍 Анализ:

  • Скорость: Обычный режим быстрее (9000ms общее время)
  • RAG использовал 2 фрагментов из документов
  • Ответ с RAG основан на реальных данных из: docker.md

💡 Вывод:
  RAG помог найти конкретную информацию в документах.
  Ответ с RAG более фактологичен и подкреплён источниками.
```

**Показать:**
- ✅ Два ответа на один вопрос
- ✅ БЕЗ RAG: общий ответ на основе знаний модели
- ✅ С RAG: конкретный ответ на основе наших документов
- ✅ Детальная статистика обоих режимов
- ✅ Анализ и выводы

**Объяснить:**
> "Инструмент compare_rag_answers показывает:
> 1. Как LLM отвечает БЕЗ доп. контекста (общие знания)
> 2. Как LLM отвечает С RAG (конкретные данные из документов)
> 3. Разница во времени, токенах и качестве ответа
> 4. RAG делает ответы более фактологичными и привязанными к источникам"

---

### Часть 5: Демонстрация отсутствия информации (1 мин)

**Промпт 4 (✅ Рабочий):**
```
Compare RAG answers for: "What is Kubernetes?"
```

**Ожидаемый результат:**
```
📊 Сравнение ответов С RAG и БЕЗ RAG

❓ Вопрос: What is Kubernetes?

🚫 БЕЗ RAG:
Kubernetes is an open-source container orchestration platform...
[Полный ответ на основе знаний модели]

✅ С RAG:
На основе предоставленных документов, информации о Kubernetes не найдено.
Найдено фрагментов: 0

💡 Вывод:
  RAG не помог - нет релевантной информации в индексе.
  Оба ответа основаны только на знаниях модели.
```

**Показать:**
- ✅ RAG честно говорит, что информации нет
- ✅ Не выдумывает данные
- ✅ БЕЗ RAG отвечает из общих знаний

**Объяснить:**
> "Важная особенность RAG: система честно признаёт отсутствие данных
> в индексе, вместо того чтобы выдумывать информацию."

---

## Итоговый хронометраж День 17

| Часть | Время | Описание |
|-------|-------|----------|
| Подготовка | 30 сек | Проверка индекса |
| Запрос с RAG #1 | 1.5 мин | "What is RAG?" с ask_with_rag |
| Запрос с RAG #2 | 1 мин | "Docker tools?" с ask_with_rag |
| Сравнение ответов | 2 мин | compare_rag_answers для Docker |
| Нет данных в RAG | 1 мин | Kubernetes пример |

**Всего: ~5 минут**

---

## Ключевые моменты для озвучки

### ask_with_rag
1. **Автоматизация** — не нужно вручную искать и объединять
2. **Статистика** — полная информация о найденных фрагментах
3. **Источники** — видно из каких файлов взята информация
4. **Релевантность** — cosine similarity показывает качество поиска
5. **Токены** — контроль расхода токенов

### compare_rag_answers
1. **Параллельное сравнение** — оба режима в одном ответе
2. **Объективность** — видна разница между RAG и не-RAG
3. **Анализ** — автоматические выводы о пользе RAG
4. **Прозрачность** — полная статистика обоих запросов

---

## Checklist День 17

- [ ] Индекс проиндексирован (9 документов)
- [ ] Backend с новыми инструментами задеплоен
- [ ] Проверены ask_with_rag и compare_rag_answers через curl
- [ ] Desktop приложение запущено
- [ ] Подготовлены промпты (английский + императивная форма)

---

## Быстрый тест перед записью День 17

```bash
# 1. Проверка ask_with_rag
curl -s -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "ask_with_rag", "arguments": "{\"question\": \"What is RAG?\"}"}' \
  | python3 -c "import sys, json; d=json.load(sys.stdin); print('✅ SUCCESS' if d['success'] else '❌ FAILED')"

# 2. Проверка compare_rag_answers
curl -s -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "compare_rag_answers", "arguments": "{\"question\": \"What is Docker?\"}"}' \
  | python3 -c "import sys, json; d=json.load(sys.stdin); print('✅ SUCCESS' if d['success'] and 'БЕЗ RAG' in d['result'] and 'С RAG' in d['result'] else '❌ FAILED')"
```

Если обе команды вернули ✅ SUCCESS — можно записывать День 17!
