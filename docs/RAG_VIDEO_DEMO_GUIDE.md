# 🎥 Инструкция для видео-демонстрации RAG системы

## Что показать на видео (5-7 минут)

### Подготовка

**Автоматическая подготовка (рекомендуется):**

1. **Запустить скрипт подготовки**
   ```bash
   cd /Users/defendend/horobot/AiCompose
   ./scripts/setup_rag_demo.sh
   ```

   Скрипт автоматически:
   - Создаёт `/opt/aicompose/demo_docs/` на сервере
   - Генерирует 3 тестовых документа (docker.md, rag.md, history.md)
   - Устанавливает правильные права (aicompose:aicompose)
   - Проверяет результат

2. **Запустить Desktop приложение**
   ```bash
   ./gradlew :desktop:run
   ```

**Созданные документы:**
- `docker.md` (1.2 KB) - Docker инструменты с примерами
- `rag.md` (1.8 KB) - RAG система, архитектура, параметры
- `history.md` (2.1 KB) - Исторические инструменты агента

---

<details>
<summary><b>Ручная подготовка (если скрипт не работает)</b></summary>

```bash
ssh defendend@89.169.190.22

# Создать директорию
sudo mkdir -p /opt/aicompose/demo_docs

# Docker документ
sudo tee /opt/aicompose/demo_docs/docker.md > /dev/null << 'EOF'
# Docker Инструменты
Docker позволяет агенту управлять контейнерами:
- docker_run - запуск контейнеров (nginx, postgres, redis)
- docker_exec - выполнение команд
- docker_logs - просмотр логов
- docker_stop - остановка
- docker_ps - список контейнеров
EOF

# RAG документ
sudo tee /opt/aicompose/demo_docs/rag.md > /dev/null << 'EOF'
# RAG Система
RAG (Retrieval-Augmented Generation):
- Индексация документов (чанки 500 символов)
- TF-IDF эмбеддинги (локально)
- Семантический поиск (косинусное сходство)
- JSON хранение
EOF

# History документ
sudo tee /opt/aicompose/demo_docs/history.md > /dev/null << 'EOF'
# Исторические Инструменты
Профессор Архивариус знает историю:
- get_historical_events - события по годам
- get_historical_figure - биографии великих
- compare_eras - сравнение эпох
- get_historical_quote - цитаты
EOF

# Установить права
sudo chown -R aicompose:aicompose /opt/aicompose/demo_docs
```
</details>

---

## Сценарий видео

### Часть 1: Введение (30 сек)

**Показать:**
- Открыт Desktop чат AiCompose
- Соединение с сервером работает
- Пустая история сообщений

**Сказать/написать:**
> "Демонстрация RAG системы в AiCompose. RAG позволяет агенту индексировать документы и искать в них информацию."

---

### Часть 2: Индексация документов (1 мин)

**Промпт 1:**
```
Проиндексируй документы из директории /opt/aicompose/demo_docs
```

**Что должно произойти:**
- Агент может ответить текстом (уточняющие вопросы) или вызвать `rag_index_documents`
- Если агент спрашивает — уточнить: "Используй инструмент rag_index_documents с path=/opt/aicompose/demo_docs"

**Ожидаемый результат:**
```
📚 Индексация завершена

Обработано:
- Путь: /opt/aicompose/demo_docs
- Файлов: 3
- Чанков: 3
- Индекс: /opt/aicompose/document_index.json

Примеры источников:
  • docker.md
  • rag.md
  • history.md

Теперь можно искать по документам через rag_search!
```

**Показать:**
- Скриншот ответа агента с результатом индексации
- Выделить количество файлов и чанков

---

### Часть 3: Проверка индекса (30 сек)

**Промпт 2:**
```
Покажи информацию о проиндексированных документах
```

**Ожидаемый результат:**
```
📊 Информация об индексе

Статус: Активен ✅
Документов: 3
Файл индекса: /opt/aicompose/document_index.json
Размер файла: 4 KB
```

**Показать:**
- Статус индекса (активен)
- Количество документов

---

### Часть 4: Семантический поиск #1 - Docker (1 мин)

**Промпт 3:**
```
Найди информацию про Docker инструменты в проиндексированных документах
```

**Ожидаемый результат:**
```
🔍 Поиск по запросу: "Docker инструменты"

Найдено 1 релевантных фрагментов:

--- Результат 1 (релевантность: 0.89) ---
Источник: docker.md

# Docker Инструменты

Docker позволяет агенту управлять контейнерами:
- docker_run - запуск контейнеров (nginx, postgres, redis)
- docker_exec - выполнение команд внутри контейнера
...
```

**Показать:**
- Релевантность поиска (0.89 — высокая)
- Найденный фрагмент из docker.md
- Выделить, что система нашла правильный документ

---

### Часть 5: Семантический поиск #2 - RAG (1 мин)

**Промпт 4:**
```
Найди что говорится про эмбеддинги и векторный поиск
```

**Ожидаемый результат:**
```
🔍 Поиск по запросу: "эмбеддинги векторный поиск"

Найдено 1 релевантных фрагментов:

--- Результат 1 (релевантность: 0.76) ---
Источник: rag.md

# RAG Система

RAG (Retrieval-Augmented Generation) позволяет агенту работать с документами:
- Индексация документов с разбивкой на чанки по 500 символов
- Генерация TF-IDF эмбеддингов (локально, без внешних API)
- Семантический поиск по косинусному сходству векторов
...
```

**Показать:**
- Система нашла информацию в rag.md
- Семантический поиск работает (похожие слова находятся)

---

### Часть 6: Семантический поиск #3 - История (1 мин)

**Промпт 5:**
```
Что есть в документах про исторические события и биографии?
```

**Ожидаемый результат:**
```
🔍 Поиск по запросу: "исторические события биографии"

Найдено 1 релевантных фрагментов:

--- Результат 1 (релевантность: 0.82) ---
Источник: history.md

# Исторические Инструменты

Профессор Архивариус знает историю:
- get_historical_events - события по годам (1945, 1961, 1991)
- get_historical_figure - биографии великих людей (Наполеон, Цезарь, Клеопатра)
...
```

**Показать:**
- Поиск нашёл history.md
- RAG понимает синонимы (события ≈ historical events)

---

### Часть 7: Комбинированный запрос (1 мин)

**Промпт 6:**
```
Какие инструменты есть у агента для работы с контейнерами?
```

**Ожидаемый результат:**
- Агент использует `rag_search` с query "контейнеры"
- Находит docker.md
- Отвечает на основе найденной информации:

```
🔍 Поиск по запросу: "контейнеры"

Найдено фрагмент из docker.md:
- docker_run - запуск контейнеров
- docker_exec - выполнение команд
- docker_logs - просмотр логов
- docker_stop - остановка
- docker_ps - список контейнеров
```

**Показать:**
- Агент использует RAG для ответа на вопрос
- Система автоматически выбирает правильный инструмент

---

### Часть 8: Заключение (30 сек)

**Сказать/написать:**
> "RAG система работает! Агент может индексировать документы, искать информацию и отвечать на вопросы на основе проиндексированных данных."

**Показать:**
- Финальный экран чата со всеми сообщениями
- Подчеркнуть: локальные эмбеддинги (TF-IDF), быстрый поиск, JSON хранение

---

## План Б: Если агент не вызывает инструменты

### Проблема
DeepSeek модель может выбрать ответить текстом вместо вызова RAG инструментов.

### Решение
Использовать новый endpoint **POST /api/tools/execute** для прямого вызова:

**Пример через curl (показать в терминале):**

1. **Индексация:**
   ```bash
   curl -X POST http://89.169.190.22/api/tools/execute \
     -H 'Content-Type: application/json' \
     -d '{
       "tool": "rag_index_documents",
       "arguments": "{\"path\": \"/opt/aicompose/demo_docs\", \"extensions\": \"md\"}"
     }'
   ```

2. **Поиск:**
   ```bash
   curl -X POST http://89.169.190.22/api/tools/execute \
     -H 'Content-Type: application/json' \
     -d '{
       "tool": "rag_search",
       "arguments": "{\"query\": \"Docker\", \"top_k\": 2}"
     }'
   ```

3. **Информация:**
   ```bash
   curl -X POST http://89.169.190.22/api/tools/execute \
     -H 'Content-Type: application/json' \
     -d '{
       "tool": "rag_index_info",
       "arguments": "{}"
     }'
   ```

**Для видео:**
- Показать терминал с curl запросами
- Показать JSON ответы
- Объяснить, что это прямой вызов инструментов (минуя LLM)

---

## Технические детали для озвучки

1. **TF-IDF векторы** — локальные, не требуют внешних API
2. **Чанки по 500 символов** — оптимальный размер для поиска
3. **Косинусное сходство** — метрика релевантности (0.0 - 1.0)
4. **JSON индекс** — лёгкое хранение, быстрая загрузка
5. **3 инструмента** — `rag_index_documents`, `rag_search`, `rag_index_info`

---

## Итоговый хронометраж

| Часть | Время | Описание |
|-------|-------|----------|
| Введение | 30 сек | Что такое RAG |
| Индексация | 1 мин | Создание индекса |
| Проверка | 30 сек | Информация об индексе |
| Поиск Docker | 1 мин | Семантический поиск #1 |
| Поиск RAG | 1 мин | Семантический поиск #2 |
| Поиск История | 1 мин | Семантический поиск #3 |
| Комбинированный | 1 мин | Агент отвечает с RAG |
| Заключение | 30 сек | Итоги |

**Всего: ~6.5 минут**

---

## Checklist перед записью

- [ ] Desktop приложение запущено
- [ ] Соединение с сервером работает
- [ ] Тестовые документы созданы в /opt/aicompose/demo_docs
- [ ] Backend с RAG инструментами задеплоен
- [ ] Проверен health check сервера
- [ ] Подготовлен Plan Б с curl (если агент не вызывает tools)
- [ ] Экран чист, нет лишних окон

---

## Дополнительно: Краш-тест для демо

Если нужно быстро проверить работу перед записью:

```bash
# Проверка сервера
curl http://89.169.190.22/api/health

# Список инструментов (новый endpoint)
curl http://89.169.190.22/api/tools/list

# Быстрый тест индексации
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "rag_index_documents", "arguments": "{\"path\": \"/opt/aicompose/demo_docs\"}"}'

# Быстрый тест поиска
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "rag_search", "arguments": "{\"query\": \"Docker\"}"}'
```

Если все 4 команды выполнились успешно — можно записывать видео!
