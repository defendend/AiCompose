# 🎬 Сценарий видео-демонстрации Docker Integration

## Цель
Показать как AI агент управляет реальным Docker окружением на сервере через Desktop приложение.

## Подготовка (перед записью)

### 1. Убедиться что Desktop приложение запущено
```bash
cd /Users/defendend/horobot/AiCompose
./gradlew :desktop:run
```

### 2. Подготовить окна для записи
- Desktop приложение AiCompose (чат)
- Терминал с SSH подключением к серверу (для показа результатов)
- Браузер (для проверки nginx)

### 3. Очистить окружение на сервере
```bash
ssh -l defendend 89.169.190.22 "docker ps -a -q | xargs -r docker rm -f"
```

## Сценарий демонстрации (5-7 минут)

### Часть 1: Введение (30 секунд)

**Что показать:**
- Открыто Desktop приложение AiCompose
- Чистый чат, готовый к работе

**Что сказать:**
> "Покажу как AI агент может управлять реальным Docker окружением на сервере.
> У агента есть 5 Docker инструментов: запуск контейнеров, выполнение команд,
> получение логов, остановка и просмотр статуса."

---

### Часть 2: Проверка текущего состояния (30 секунд)

**Промпт в чат:**
```
Покажи список всех запущенных docker контейнеров
```

**Что показать:**
- Агент вызывает инструмент `docker_ps`
- Ответ: "Нет запущенных контейнеров" (пустая гавань)
- В UI видна карточка с инструментом (если добавлена визуализация)

**Что сказать:**
> "Сначала проверим что на сервере нет запущенных контейнеров.
> Агент вызывает docker_ps и подтверждает - всё чисто."

---

### Часть 3: Запуск веб-сервера (1.5 минуты)

**Промпт в чат:**
```
Запусти nginx контейнер на порту 8888 с именем demo-nginx
```

**Что показать:**
1. Агент вызывает `docker_run` с параметрами:
   - image: nginx:alpine
   - name: demo-nginx
   - ports: 8888:80
2. Успешный запуск, показан ID контейнера
3. **ПЕРЕКЛЮЧИТЬСЯ НА ТЕРМИНАЛ** и выполнить:
   ```bash
   ssh defendend@89.169.190.22 "docker ps"
   ```
   Показать что контейнер действительно запущен

**Что сказать:**
> "Агент запускает nginx контейнер. Видите - он создал контейнер с именем demo-nginx
> и пробросил порт 8888 на внутренний порт 80. Проверим в терминале -
> контейнер действительно запустился на сервере!"

---

### Часть 4: Проверка работы веб-сервера (1.5 минуты)

**Промпт в чат:**
```
Выполни команду curl localhost внутри контейнера demo-nginx
```

**Что показать:**
1. Агент вызывает `docker_exec` с командой curl
2. Получает HTML ответ от nginx
3. **ПЕРЕКЛЮЧИТЬСЯ НА БРАУЗЕР**:
   - Открыть `http://89.169.190.22:8888`
   - Показать страницу "Welcome to nginx!"

**Что сказать:**
> "Агент проверяет работу nginx изнутри контейнера через curl.
> Получили HTML ответ - сервер работает! Проверим в браузере -
> да, nginx отвечает на запросы из интернета!"

---

### Часть 5: Просмотр логов (1 минута)

**Промпт в чат:**
```
Покажи последние логи контейнера demo-nginx
```

**Что показать:**
1. Агент вызывает `docker_logs`
2. Видны логи запуска nginx:
   - Версия nginx
   - Количество worker processes
   - Сообщения о готовности

**Что сказать:**
> "Агент получает логи контейнера. Видим что nginx версии 1.29 запустился
> с 4 рабочими процессами и готов обрабатывать запросы."

---

### Часть 6: Сложный сценарий - Запуск базы данных (2 минуты)

**Промпт в чат:**
```
Запусти PostgreSQL контейнер с паролем testpass123 на порту 5433,
затем выполни простой SQL запрос для проверки
```

**Что показать:**
1. Агент запускает postgres:15 с переменной окружения POSTGRES_PASSWORD
2. Проброс порта 5433:5432
3. Агент ждет ~5 секунд и выполняет команду:
   ```
   docker exec <container> psql -U postgres -c "SELECT version()"
   ```
4. Получает версию PostgreSQL

**Что сказать:**
> "Агент может работать не только с веб-серверами, но и с базами данных.
> Запускаем PostgreSQL с паролем и проверяем подключение через SQL запрос.
> База данных готова к работе!"

---

### Часть 7: Проверка всех контейнеров (30 секунд)

**Промпт в чат:**
```
Покажи список всех запущенных контейнеров
```

**Что показать:**
1. Агент вызывает `docker_ps`
2. Показывает оба контейнера:
   - demo-nginx (порт 8888)
   - postgres (порт 5433)
3. С полной информацией: ID, образ, статус, порты

**Что сказать:**
> "Теперь у нас два рабочих контейнера - веб-сервер и база данных.
> Агент показывает полную информацию: какие порты открыты,
> сколько времени работают."

---

### Часть 8: Очистка (1 минута)

**Промпт в чат:**
```
Останови и удали все контейнеры
```

**Что показать:**
1. Агент останавливает demo-nginx (docker_stop)
2. Агент останавливает postgres (docker_stop)
3. Оба контейнера удалены
4. **ПЕРЕКЛЮЧИТЬСЯ НА ТЕРМИНАЛ**:
   ```bash
   ssh defendend@89.169.190.22 "docker ps"
   ```
   Показать что контейнеров нет

**Что сказать:**
> "Агент останавливает и удаляет оба контейнера. Проверяем в терминале -
> всё чисто, окружение возвращено в исходное состояние."

---

### Часть 9: Демонстрация Pipeline (1 минута) - БОНУС

**Промпт в чат:**
```
Найди документы про compose, суммаризируй и сохрани результат
```

**Что показать:**
1. Агент вызывает 3 Pipeline инструмента последовательно:
   - 🔍 pipeline_search_docs
   - 📝 pipeline_summarize
   - 💾 pipeline_save_to_file
2. В UI видны красивые карточки для каждого шага (из Day 14)
3. Файл сохранен в pipeline_results/

**Что сказать:**
> "А это бонус - агент может комбинировать разные инструменты.
> Например, найти документы, создать сводку и сохранить в файл.
> Всё автоматически, в одной цепочке!"

---

### Часть 10: Заключение (30 секунд)

**Что показать:**
- Финальный экран с чатом
- Можно показать полный список инструментов в настройках

**Что сказать:**
> "Итого: AI агент управляет реальным Docker окружением на сервере.
> Он может запускать любые контейнеры, проверять их работу,
> получать логи и управлять жизненным циклом. Всё это из простого
> Desktop приложения на Compose Multiplatform!"

---

## Технические детали для записи

### Настройки записи экрана
- **Разрешение**: 1920x1080 или 2560x1440
- **FPS**: 30
- **Область записи**: Полный экран или Desktop окно + терминал
- **Курсор**: Показывать

### Раскладка окон
```
┌────────────────────────────────────────┐
│  Desktop App (AiCompose)               │
│  Чат с агентом                         │
│                                        │
├────────────────────────────────────────┤
│  Terminal (SSH к серверу)              │
│  docker ps, curl и т.д.                │
└────────────────────────────────────────┘
```

### Подсветка
- Используйте подсветку курсора (например, через Keycastr на Mac)
- Увеличьте размер шрифта в чате и терминале для лучшей читаемости

### Таймлайн записи
| Время | Действие | Инструмент |
|-------|----------|------------|
| 0:00-0:30 | Введение | - |
| 0:30-1:00 | Проверка статуса | docker_ps |
| 1:00-2:30 | Запуск nginx | docker_run |
| 2:30-4:00 | Проверка работы | docker_exec |
| 4:00-5:00 | Просмотр логов | docker_logs |
| 5:00-7:00 | PostgreSQL сценарий | docker_run + docker_exec |
| 7:00-7:30 | Список контейнеров | docker_ps |
| 7:30-8:30 | Очистка | docker_stop |
| 8:30-9:30 | Pipeline бонус | pipeline_* |
| 9:30-10:00 | Заключение | - |

### Советы по монтажу
1. Добавьте текстовые выноски для ключевых моментов:
   - "🐳 Агент запускает nginx..."
   - "✅ Контейнер успешно запущен"
   - "🌐 Проверяем в браузере..."

2. Можно ускорить длительные операции (ожидание запуска контейнера)

3. Добавьте финальный слайд с итогами:
   ```
   AI Agent + Docker = 🔥

   ✅ 5 Docker инструментов
   ✅ Управление реальным окружением
   ✅ Запуск любых сервисов
   ✅ Из простого Desktop UI

   github.com/defendend/AiCompose
   ```

## Альтернативный короткий сценарий (2 минуты)

Если нужно быстрее:

1. **"Запусти nginx на 8888 и покажи что он работает"** (30 сек)
2. **Проверка в браузере** (15 сек)
3. **"Запусти redis и выполни SET/GET"** (45 сек)
4. **"Останови все контейнеры"** (30 сек)

## Готовность к записи - Чеклист

- [ ] Desktop приложение запущено
- [ ] SSH сессия открыта
- [ ] Браузер готов (вкладка http://89.169.190.22:8888)
- [ ] Все контейнеры на сервере остановлены
- [ ] Размер шрифта увеличен в UI
- [ ] Программа записи экрана готова
- [ ] Микрофон проверен (если будет голос)
- [ ] План перед глазами

**Время на полную демонстрацию: ~10 минут**
**Время на короткую демонстрацию: ~2 минуты**

Удачи! 🎬🔥
