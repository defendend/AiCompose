# ⚡ Быстрый чеклист для видео-демо Docker

## Перед записью (5 минут подготовки)

### 1. Запустить Desktop приложение
```bash
cd /Users/defendend/horobot/AiCompose
./gradlew :desktop:run
```

### 2. Очистить Docker на сервере
```bash
ssh defendend@89.169.190.22 "docker ps -a -q | xargs -r docker rm -f"
```

### 3. Проверить что всё работает (быстрый тест)

В Desktop чате написать:
```
Покажи список docker контейнеров
```

Ожидаемый ответ: "Нет запущенных контейнеров"

---

## Готовые промпты для демо (копируй-вставляй)

### Вариант 1: Полная демонстрация (10 минут)

```
1. Покажи список всех запущенных docker контейнеров

2. Запусти nginx контейнер на порту 8888 с именем demo-nginx

3. Выполни команду curl localhost внутри контейнера demo-nginx

4. Покажи последние логи контейнера demo-nginx

5. Запусти PostgreSQL контейнер с паролем testpass123 на порту 5433

6. Покажи список всех запущенных контейнеров

7. Останови и удали контейнер demo-nginx

8. Останови и удали контейнер postgres

9. Найди документы про compose, суммаризируй и сохрани результат
```

### Вариант 2: Быстрая демонстрация (3 минуты)

```
1. Запусти nginx на порту 8888, проверь что он работает и покажи логи

2. Запусти redis контейнер с именем demo-redis

3. Покажи все запущенные контейнеры

4. Останови все контейнеры
```

### Вариант 3: Эффектная демонстрация (5 минут)

```
1. Создай тестовое окружение: запусти nginx на 8080, postgres с паролем test123 и redis. Проверь что все сервисы работают

2. Покажи статус всех контейнеров с полной информацией

3. Останови и удали все контейнеры
```

---

## Проверка в браузере

После запуска nginx, открыть:
```
http://89.169.190.22:8888
```

Должна появиться страница "Welcome to nginx!"

---

## SSH команды для показа результатов

### Показать запущенные контейнеры
```bash
ssh defendend@89.169.190.22 "docker ps"
```

### Проверить nginx
```bash
ssh defendend@89.169.190.22 "curl -s http://localhost:8888 | head -5"
```

### Показать использование ресурсов
```bash
ssh defendend@89.169.190.22 "docker stats --no-stream"
```

---

## Настройки экрана для записи

- [ ] Увеличить шрифт в Desktop UI (Settings → Appearance)
- [ ] Открыть терминал рядом с Desktop окном
- [ ] Подготовить браузер с вкладкой http://89.169.190.22:8888
- [ ] Включить подсветку курсора (Keycastr для Mac)
- [ ] Проверить микрофон (если будет озвучка)

---

## Структура окон для записи

```
┌─────────────────────────────────────┐
│                                     │
│    Desktop App (AiCompose)          │
│    1920x1080 или больше             │
│                                     │
└─────────────────────────────────────┘

┌──────────────────┬──────────────────┐
│  Desktop App     │  Terminal/Browser│
│  Чат             │  Проверка        │
│  (70%)           │  (30%)           │
└──────────────────┴──────────────────┘
```

---

## Тайминг для короткой версии (2-3 минуты)

| Время | Действие |
|-------|----------|
| 0:00 | Показ списка контейнеров (пусто) |
| 0:15 | Запуск nginx на 8888 |
| 0:45 | Проверка в браузере |
| 1:15 | Запуск postgres или redis |
| 1:45 | Список всех контейнеров |
| 2:15 | Остановка всех контейнеров |
| 2:45 | Финальная проверка (пусто) |

---

## Что говорить (скрипт на русском)

### Начало
> "Покажу как AI агент управляет Docker контейнерами на реальном сервере.
> У агента 5 инструментов для работы с Docker."

### Запуск nginx
> "Запускаем веб-сервер nginx. Агент создаёт контейнер, пробрасывает порт
> и показывает ID. Проверяем в браузере - работает!"

### Запуск базы данных
> "Теперь база данных PostgreSQL. Агент передаёт пароль через переменную
> окружения и проверяет подключение SQL запросом."

### Остановка
> "Агент останавливает и удаляет все контейнеры. Окружение чистое!"

### Заключение
> "AI агент полностью управляет Docker окружением: запускает, проверяет,
> мониторит и останавливает. Всё из Desktop приложения!"

---

## После записи - Монтаж

1. Добавить текстовые выноски:
   - "🐳 Запуск nginx..."
   - "✅ Проверка в браузере"
   - "📊 Статус контейнеров"
   - "🛑 Очистка окружения"

2. Ускорить длинные паузы (ожидание запуска)

3. Добавить финальный слайд:
   ```
   AI Agent + Docker = 🔥

   ✅ Управление реальным окружением
   ✅ 5 Docker инструментов
   ✅ Любые сервисы из чата

   GitHub: defendend/AiCompose
   ```

---

## Возможные проблемы и решения

**Проблема**: Контейнер не запускается
**Решение**: Проверить что порт свободен: `docker ps`

**Проблема**: Nginx не отвечает в браузере
**Решение**: Подождать 2-3 секунды после запуска

**Проблема**: Агент не видит Docker
**Решение**: Перезапустить backend: `ssh defendend@89.169.190.22 "sudo systemctl restart aicompose"`

---

## Готов к записи? ✅

- [ ] Desktop приложение запущено
- [ ] Docker на сервере чист
- [ ] Промпты подготовлены
- [ ] Окна расположены правильно
- [ ] Шрифт увеличен
- [ ] Браузер готов
- [ ] План перед глазами

**Поехали! 🎬**
