# 🎨 Визуализация Pipeline Инструментов

## Описание

Красивое отображение композиции MCP-инструментов в Desktop UI. Каждый шаг пайплайна показывается в виде раскрываемой карточки с иконкой, параметрами и результатом.

## Внешний вид

### Свёрнутый вид (по умолчанию)

```
┌─────────────────────────────────────────────────────────┐
│ 🔍  Поиск документов                    [Детали ▼]      │
│     Шаг пайплайна                                        │
│                                                          │
│ 📥 Входные параметры:                                    │
│    query: "kotlin"                                       │
│                                                          │
│ ✅ Выполнено успешно                                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 📝  Суммаризация                        [Детали ▼]      │
│     Шаг пайплайна                                        │
│                                                          │
│ 📥 Входные параметры:                                    │
│    text: "🔍 Поиск по запросу: \"kotlin\"                │
│           Найдено документов: 3..."                      │
│                                                          │
│ ✅ Выполнено успешно                                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 💾  Сохранение в файл                   [Детали ▼]      │
│     Шаг пайплайна                                        │
│                                                          │
│ 📥 Входные параметры:                                    │
│    content: "📝 Суммаризация завершена..."               │
│    filename: "kotlin_summary.txt"                        │
│                                                          │
│ ✅ Выполнено успешно                                     │
└─────────────────────────────────────────────────────────┘
```

### Развёрнутый вид (после нажатия "Детали")

```
┌─────────────────────────────────────────────────────────┐
│ 🔍  Поиск документов                    [Скрыть ▲]      │
│     Шаг пайплайна                                        │
│                                                          │
│ 📥 Входные параметры:                                    │
│    query: "kotlin"                                       │
│                                                          │
│ ✅ Результат выполнения:                                 │
│    🔍 Поиск по запросу: "kotlin"                         │
│                                                          │
│    Найдено документов: 3                                 │
│                                                          │
│    • [kotlin] Kotlin — современный язык программиро-     │
│      вания для JVM, Android и веб-разработки.            │
│    • [kotlin] Kotlin поддерживает null-safety, corou-    │
│      tines, extension functions и data classes.          │
│    • [kotlin] Kotlin был создан JetBrains в 2011 году    │
│      и стал официальным языком для Android в 2017.       │
└─────────────────────────────────────────────────────────┘
```

## Цветовая схема

| Инструмент | Иконка | Цвет |
|------------|--------|------|
| `pipeline_search_docs` | 🔍 | Primary (синий) |
| `pipeline_summarize` | 📝 | Secondary (фиолетовый) |
| `pipeline_save_to_file` | 💾 | Tertiary (зелёный) |

## Особенности реализации

### Автоматическое определение Pipeline инструментов

```kotlin
message.toolCall?.let { toolCall ->
    if (toolCall.name.startsWith("pipeline_")) {
        PipelineToolCallView(toolCall, message.toolResult, textColor)
    } else {
        // Обычная визуализация
    }
}
```

### Парсинг JSON параметров

Входные параметры автоматически парсятся из JSON в `key: value` пары:

```kotlin
val params = remember(toolCall.arguments) {
    try {
        val json = Json.parseToJsonElement(toolCall.arguments).jsonObject
        json.entries.associate { (key, value) ->
            key to value.toString().removeSurrounding("\"")
        }
    } catch (e: Exception) {
        mapOf("raw" to toolCall.arguments)
    }
}
```

### Раскрываемый результат

```kotlin
var isExpanded by remember { mutableStateOf(false) }

TextButton(onClick = { isExpanded = !isExpanded }) {
    Text(if (isExpanded) "Скрыть ▲" else "Детали ▼")
}
```

## Пример использования

### В Desktop приложении

1. Запустить приложение: `./gradlew :desktop:run`
2. В чате написать: "Найди документы про kotlin, суммаризируй и сохрани в файл kotlin_summary.txt"
3. Агент автоматически вызовет 3 инструмента:
   - 🔍 `pipeline_search_docs` с параметром `query: "kotlin"`
   - 📝 `pipeline_summarize` с результатами поиска
   - 💾 `pipeline_save_to_file` с итоговой сводкой

### Через API (для записи видео)

```bash
curl -X POST http://89.169.190.22/api/chat \
  -H 'Content-Type: application/json' \
  -d '{
    "message": "Find documents about compose, summarize and save to compose_info.txt"
  }'
```

## Для видео демонстрации

### Рекомендуемые сценарии

1. **Базовый пайплайн (3 шага)**
   ```
   "Найди документы про kotlin, суммаризируй и сохрани в файл"
   ```
   - Показывает все 3 инструмента
   - Наглядная последовательность: 🔍 → 📝 → 💾

2. **Разные темы**
   ```
   "Найди информацию про compose, сделай краткую сводку и сохрани"
   ```
   - Другие документы из базы
   - Демонстрирует универсальность

3. **MCP тема**
   ```
   "Найди про mcp, суммаризируй результаты и сохрани в mcp_summary.txt"
   ```
   - Показывает работу с третьей темой

### Советы для записи

- Включить **тёмную тему** (уже включена по умолчанию)
- Увеличить **масштаб UI** для лучшей видимости
- Раскрыть каждый шаг по очереди, чтобы показать детали
- Показать файл в `pipeline_results/` после сохранения
- Использовать **медленную прокрутку** для демонстрации

## Файлы

| Файл | Описание |
|------|----------|
| `desktop/src/main/kotlin/org/example/ui/components/ChatScreen.kt` | PipelineToolCallView composable |
| `backend/src/main/kotlin/org/example/tools/pipeline/PipelineTools.kt` | Реализация 3 инструментов |
| `.claude/CLAUDE.md` | Документация с описанием визуализации |

## Результат

Красивая пошаговая визуализация композиции MCP-инструментов прямо в чате, готовая для демонстрации на видео! 🎬
