# 📖 RAG: Примеры использования

## Прямые вызовы инструментов (без LLM)

### Kotlin код

```kotlin
import org.example.tools.rag.*
import kotlinx.coroutines.runBlocking

fun main() = runBlocking {
    // 1. Индексация документов
    val indexResult = RagIndexDocuments.execute("""
        {
            "path": "/path/to/docs",
            "extensions": "md,txt,kt"
        }
    """.trimIndent())

    println(indexResult)
    // Output:
    // 📚 Индексация завершена
    //
    // Обработано:
    // - Путь: /path/to/docs
    // - Файлов: 12
    // - Чанков: 85
    // - Индекс: /opt/aicompose/document_index.json

    // 2. Информация об индексе
    val infoResult = RagIndexInfo.execute("{}")

    println(infoResult)
    // Output:
    // 📊 Информация об индексе
    //
    // Статус: Активен ✅
    // Документов: 85
    // Файл индекса: /opt/aicompose/document_index.json

    // 3. Поиск
    val searchResult = RagSearch.execute("""
        {
            "query": "Docker инструменты",
            "top_k": 3
        }
    """.trimIndent())

    println(searchResult)
    // Output:
    // 🔍 Поиск по запросу: "Docker инструменты"
    //
    // Найдено 3 релевантных фрагментов:
    //
    // --- Результат 1 (релевантность: 0.89) ---
    // Источник: DOCKER_DEMO.md
    // ...
}
```

## Использование через API (curl)

### 1. Индексация

```bash
curl -X POST http://89.169.190.22/api/chat \
  -H 'Content-Type: application/json' \
  -d '{
    "message": "Используй rag_index_documents для индексации /tmp/test_docs"
  }'
```

**Примечание:** Модель DeepSeek может выбрать не вызывать tool, а ответить текстом. Это нормальное поведение LLM - она самостоятельно решает, когда использовать инструменты.

### 2. Поиск

```bash
curl -X POST http://89.169.190.22/api/chat \
  -H 'Content-Type: application/json' \
  -d '{
    "message": "Найди информацию про Docker в проиндексированных документах используя rag_search"
  }'
```

### 3. Информация об индексе

```bash
curl -X POST http://89.169.190.22/api/chat \
  -H 'Content-Type: application/json' \
  -d '{
    "message": "Используй rag_index_info чтобы показать информацию об индексе"
  }'
```

## Desktop приложение

В Desktop чате можно использовать естественные промпты:

1. **Индексация:**
   - "Проиндексируй документацию из директории docs"
   - "Создай индекс для всех markdown файлов в /tmp/my_docs"

2. **Поиск:**
   - "Найди информацию про Docker в проиндексированных документах"
   - "Что говорится про RAG систему в документации?"

3. **Информация:**
   - "Покажи информацию о текущем индексе документов"
   - "Сколько документов проиндексировано?"

## Интеграционный тест

Полный пример использования RAG системы в тесте:

```kotlin
@Test
fun `full RAG workflow`() = runTest {
    // Создаём тестовые документы
    val testDir = File("/tmp/rag_test")
    testDir.mkdirs()
    File(testDir, "doc1.md").writeText("Docker позволяет управлять контейнерами")
    File(testDir, "doc2.md").writeText("RAG система индексирует документы")

    // 1. Индексация
    val indexResult = RagIndexDocuments.execute("""{"path": "${testDir.absolutePath}"}""")
    assertContains(indexResult, "Индексация завершена")

    // 2. Информация
    val infoResult = RagIndexInfo.execute("{}")
    assertContains(infoResult, "Статус: Активен")

    // 3. Поиск
    val searchResult = RagSearch.execute("""{"query": "Docker", "top_k": 1}""")
    assertContains(searchResult, "контейнерами")

    // Очистка
    testDir.deleteRecursively()
    File("document_index.json").delete()
}
```

## Архитектура RAG системы

```
┌──────────────────────┐
│  DocumentChunker     │  ← Разбивает документы на чанки (500 символов, 50 перекрытие)
└──────────────────────┘
          │
          ├─ loadDocument(file)
          ├─ chunkText(text)
          └─ chunkDirectory(dir)
                  │
                  ▼
┌──────────────────────┐
│  SimpleEmbeddings    │  ← Генерирует TF-IDF векторы (локально, без API)
└──────────────────────┘
          │
          ├─ fit(documents)
          ├─ embed(text)
          └─ cosineSimilarity(vec1, vec2)
                  │
                  ▼
┌──────────────────────┐
│  DocumentIndex       │  ← Хранит индекс с эмбеддингами
└──────────────────────┘
          │
          ├─ indexChunks(chunks)
          ├─ search(query, topK)
          ├─ save(file)  → document_index.json
          └─ load(file)
                  │
                  ▼
┌──────────────────────┐
│  RagTools            │  ← 3 инструмента для агента
└──────────────────────┘
          │
          ├─ rag_index_documents
          ├─ rag_search
          └─ rag_index_info
```

## Параметры и настройки

### ChunkConfig
```kotlin
ChunkConfig(
    chunkSize = 500,        // Размер чанка в символах
    chunkOverlap = 50,      // Перекрытие между чанками
    minChunkSize = 100      // Минимальный размер чанка
)
```

### Поддерживаемые форматы
- `.md` - Markdown
- `.txt` - Текстовые файлы
- `.kt` - Kotlin
- `.java` - Java
- `.js` - JavaScript
- `.py` - Python
- `.ts` - TypeScript

### Размерность векторов
- Словарь: топ 5000 терминов по частоте
- Векторы: TF-IDF, нормализованные
- Метрика: косинусное сходство

## Производительность

| Операция | Время (3 документа) |
|----------|---------------------|
| Индексация | ~50ms |
| Поиск (top-3) | ~10ms |
| Сохранение JSON | ~5ms |
| Загрузка JSON | ~10ms |

## Ограничения

1. **TF-IDF векторы** - простые, но работают хорошо для коротких документов
2. **In-Memory** - индекс хранится в памяти (для больших объёмов лучше FAISS)
3. **JSON хранилище** - для продакшна рекомендуется PostgreSQL + pgvector

## Улучшения для продакшена

### 1. OpenAI эмбеддинги
```kotlin
// Вместо TF-IDF использовать text-embedding-3-small
val embedding = openAI.embeddings(text)
```

### 2. FAISS для быстрого поиска
```kotlin
// IndexFlatIP для косинусного сходства
val index = IndexFlatIP(dimension)
index.add(vectors)
val results = index.search(query, topK)
```

### 3. PostgreSQL + pgvector
```sql
CREATE EXTENSION vector;
CREATE TABLE documents (
  id SERIAL PRIMARY KEY,
  content TEXT,
  embedding vector(1536)
);

-- Поиск по векторам
SELECT id, content,
       embedding <#> '[...]' as distance
FROM documents
ORDER BY distance
LIMIT 5;
```

## Troubleshooting

### Модель не вызывает RAG инструменты

**Проблема:** DeepSeek решает ответить текстом вместо вызова tool.

**Решение:**
1. Используйте более явные промпты: "Use rag_index_documents tool"
2. Измените системный промпт через `customSystemPrompt`
3. Используйте прямые вызовы инструментов в коде (см. Kotlin примеры)

### Индекс не сохраняется

**Проблема:** Файл `document_index.json` не создаётся.

**Решение:**
- Проверьте права на запись в директории
- Убедитесь, что путь к документам правильный
- Проверьте, что расширения файлов совпадают с параметром `extensions`

### Поиск возвращает нерелевантные результаты

**Проблема:** TF-IDF возвращает документы с низкой релевантностью.

**Решение:**
- Увеличьте `top_k` для получения большего количества результатов
- Используйте более специфичные поисковые запросы
- Для лучших результатов переключитесь на OpenAI embeddings

## См. также

- [RAG_DEMO.md](RAG_DEMO.md) - Подробное описание RAG системы
- [PIPELINE_DEMO.md](PIPELINE_DEMO.md) - Pipeline инструменты
- [DOCKER_DEMO.md](DOCKER_DEMO.md) - Docker интеграция
