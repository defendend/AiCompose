# 🎬 День 17: Шпаргалка для записи видео

## ✅ Предварительная проверка

```bash
# Тест ask_with_rag
curl -s -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "ask_with_rag", "arguments": "{\"question\": \"What is RAG?\"}"}' \
  | python3 -c "import sys, json; d=json.load(sys.stdin); print('✅ SUCCESS' if d['success'] else '❌ FAILED')"

# Тест compare_rag_answers
curl -s -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "compare_rag_answers", "arguments": "{\"question\": \"What is Docker?\"}"}' \
  | python3 -c "import sys, json; d=json.load(sys.stdin); print('✅ SUCCESS' if d['success'] and 'БЕЗ RAG' in d['result'] else '❌ FAILED')"
```

---

## 🎥 Сценарий (5 минут)

### 1. Введение (30 сек)

**Сказать:**
> "День 17: RAG-запросы. Сегодня показываю два новых инструмента:
> ask_with_rag — для автоматического поиска и ответа с контекстом,
> и compare_rag_answers — для сравнения ответов с RAG и без."

### 2. Проверка индекса (15 сек)

**Промпт в чат:**
```
Use rag_index_info
```

**Ожидаем:** Индекс активен, 9 документов

### 3. Первый запрос с RAG (1.5 мин)

**Промпт в чат:**
```
Use ask_with_rag to answer: "What is RAG and how does it work?" with top_k=3
```

**План Б (curl в терминале):**
```bash
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "ask_with_rag", "arguments": "{\"question\": \"What is RAG and how does it work?\", \"top_k\": 3}"}'
```

**Показать:**
- ✅ Найдено 3 фрагмента из rag.md и history.md
- ✅ Ответ основан на реальных данных
- ✅ Статистика: источники, релевантность, время, токены

**Объяснить:**
> "ask_with_rag автоматически ищет релевантные фрагменты,
> объединяет их с вопросом и отправляет обогащённый запрос к LLM.
> В ответе видна полная статистика: найдено 3 фрагмента,
> релевантность 0.18, время ответа 8 секунд."

### 4. Второй запрос с RAG (1 мин)

**Промпт в чат:**
```
Ask with RAG: "What Docker tools are available?" using top_k=2
```

**План Б (curl):**
```bash
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "ask_with_rag", "arguments": "{\"question\": \"What Docker tools are available?\", \"top_k\": 2}"}'
```

**Показать:**
- ✅ Нашёл правильный docker.md
- ✅ Релевантность 0.45 (выше, так как вопрос специфичнее)

### 5. Сравнение ответов (2 мин) — ГЛАВНАЯ ФИШКА

**Промпт в чат:**
```
Use compare_rag_answers to compare: "How does Docker work?" with top_k=2
```

**План Б (curl):**
```bash
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "compare_rag_answers", "arguments": "{\"question\": \"How does Docker work?\", \"top_k\": 2}"}'
```

**Показать:**
- ✅ ДВА ответа на ОДИН вопрос
- ✅ БЕЗ RAG: общий ответ (знания модели)
- ✅ С RAG: конкретный ответ (наши документы)
- ✅ Детальная статистика обоих
- ✅ Автоматический анализ и выводы

**Объяснить:**
> "compare_rag_answers — это мощный инструмент для оценки RAG.
> Он показывает ДВА ответа параллельно:
> 1. Обычный LLM без контекста — общие знания модели
> 2. LLM с RAG — конкретные данные из наших документов
>
> Система автоматически анализирует:
> - Скорость ответа
> - Количество найденных фрагментов
> - Источники информации
> - Разницу в токенах
>
> И делает вывод: помог ли RAG в данном случае."

### 6. Демонстрация честности RAG (30 сек)

**Промпт в чат:**
```
Compare RAG answers for: "What is Kubernetes?"
```

**Показать:**
- ✅ С RAG: "Найдено фрагментов: 0"
- ✅ RAG честно признаёт отсутствие данных
- ✅ Не выдумывает информацию

**Объяснить:**
> "Важная особенность: RAG не галлюцинирует.
> Если информации нет в индексе — система честно об этом говорит."

### 7. Заключение (30 сек)

**Сказать:**
> "День 17 завершён! Реализован полный pipeline RAG-запросов:
> - ask_with_rag — автоматический поиск и ответ с контекстом
> - compare_rag_answers — сравнение с RAG и без RAG
>
> Система показывает:
> - Источники информации
> - Релевантность (cosine similarity)
> - Время и токены
> - Автоматический анализ эффективности RAG
>
> Всего в системе теперь 29 инструментов, 5 из них — RAG!"

---

## 🔑 Ключевые фразы для озвучки

### ask_with_rag
- "Автоматически ищет релевантные фрагменты"
- "Объединяет контекст с вопросом"
- "Обогащённый запрос к LLM"
- "Полная статистика: источники, релевантность, время, токены"

### compare_rag_answers
- "ДВА ответа на ОДИН вопрос"
- "БЕЗ RAG — общие знания модели"
- "С RAG — конкретные данные из документов"
- "Автоматический анализ эффективности"
- "RAG делает ответы фактологичными и подкреплёнными источниками"

---

## 📋 Checklist перед записью

- [ ] Backend задеплоен (29 инструментов)
- [ ] Индекс проиндексирован (9 документов)
- [ ] Оба теста прошли ✅
- [ ] Desktop приложение запущено
- [ ] Экран чист, нет лишних окон
- [ ] Микрофон работает
- [ ] План Б подготовлен (curl команды)

---

## 🚀 План Б: Терминал вместо чата

Если агент не вызывает инструменты в чате, используй curl:

**ask_with_rag:**
```bash
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "ask_with_rag", "arguments": "{\"question\": \"What is RAG?\", \"top_k\": 3}"}'
```

**compare_rag_answers:**
```bash
curl -X POST http://89.169.190.22/api/tools/execute \
  -H 'Content-Type: application/json' \
  -d '{"tool": "compare_rag_answers", "arguments": "{\"question\": \"How does Docker work?\"}"}'
```

---

## 💡 Что подчеркнуть

1. **Автоматизация** — не нужно вручную искать, копировать, вставлять
2. **Прозрачность** — видно ВСЁ: источники, релевантность, токены
3. **Сравнение** — объективная оценка пользы RAG
4. **Честность** — система не выдумывает, если данных нет
5. **Статистика** — всё измеримо и контролируемо

**Главное:** День 17 превращает RAG из "поиска" в "умного ассистента с памятью"!
