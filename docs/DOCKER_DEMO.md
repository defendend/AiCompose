# 🐳 Docker Environment Integration - Demo

## Описание

Агент может управлять Docker контейнерами на сервере, создавая реальное окружение для тестирования и проверки приложений.

## Доступные инструменты

| Инструмент | Описание |
|------------|----------|
| `docker_run` | Запускает Docker контейнер с указанным образом |
| `docker_exec` | Выполняет команду внутри запущенного контейнера |
| `docker_logs` | Получает логи контейнера |
| `docker_stop` | Останавливает и удаляет контейнер |
| `docker_ps` | Показывает список запущенных контейнеров |

## Демо-сценарии

### Сценарий 1: Запуск веб-сервера (Nginx)

**Промпт для агента:**
```
Запусти nginx контейнер на порту 8888, проверь что он работает и покажи логи
```

**Что делает агент:**
1. `docker_run` — запускает nginx:alpine с пробросом порта 8888:80
2. `docker_ps` — проверяет что контейнер запустился
3. `docker_exec` — выполняет `curl localhost` внутри контейнера
4. `docker_logs` — получает логи nginx

**Проверка результата:**
```bash
# На сервере
curl http://localhost:8888
# Должен вернуть HTML страницу nginx
```

### Сценарий 2: Запуск базы данных (PostgreSQL)

**Промпт для агента:**
```
Запусти PostgreSQL контейнер с паролем 'testpass', создай тестовую таблицу и выполни запрос
```

**Что делает агент:**
1. `docker_run` — запускает postgres:15 с переменной POSTGRES_PASSWORD
2. `docker_exec` — создаёт таблицу через psql
3. `docker_exec` — выполняет SELECT запрос
4. `docker_logs` — показывает логи инициализации

### Сценарий 3: Запуск Redis и тестирование

**Промпт для агента:**
```
Запусти Redis, сохрани тестовое значение и прочитай его обратно
```

**Что делает агент:**
1. `docker_run` — запускает redis:alpine
2. `docker_exec` — выполняет `redis-cli SET test "Hello from AI Agent"`
3. `docker_exec` — выполняет `redis-cli GET test`
4. `docker_logs` — показывает активность Redis

### Сценарий 4: CI/CD Pipeline

**Промпт для агента:**
```
Создай окружение для тестирования: запусти nginx, postgres и redis, проверь что все работает
```

**Что делает агент:**
1. Запускает 3 контейнера с разными сервисами
2. Проверяет доступность каждого сервиса
3. Выполняет health checks
4. Показывает статус всех контейнеров

## API примеры

### Запуск nginx

```bash
curl -X POST http://89.169.190.22/api/chat \
  -H 'Content-Type: application/json' \
  -d '{
    "message": "Run nginx container on port 8888, check if it works and show me the logs"
  }'
```

### Запуск PostgreSQL

```bash
curl -X POST http://89.169.190.22/api/chat \
  -H 'Content-Type: application/json' \
  -d '{
    "message": "Start postgres container with password testpass and create a test table"
  }'
```

### Полный pipeline

```bash
curl -X POST http://89.169.190.22/api/chat \
  -H 'Content-Type: application/json' \
  -d '{
    "message": "Create a testing environment: start nginx on 8080, postgres with password test123, and redis. Then verify all services are running"
  }'
```

## Desktop UI

В Desktop приложении Docker инструменты отображаются с уникальными иконками:
- 🐳 `docker_run` — Запуск контейнера
- ⚙️ `docker_exec` — Выполнение команды
- 📋 `docker_logs` — Просмотр логов
- 🛑 `docker_stop` — Остановка контейнера
- 📦 `docker_ps` — Список контейнеров

## Безопасность

**Важно:**
- Контейнеры запускаются на сервере backend (89.169.190.22)
- Используются только публичные образы из Docker Hub
- Контейнеры автоматически удаляются после остановки
- Нет доступа к файловой системе хоста (без volume mounts)

## Ограничения

- Таймаут выполнения команд: 30 секунд
- Максимальный размер логов: 50 строк (tail)
- Только detached режим (фоновый запуск)

## Примеры образов

| Образ | Использование |
|-------|---------------|
| nginx:alpine | Веб-сервер для статических файлов |
| postgres:15 | База данных для тестирования |
| redis:alpine | Кэш и очереди сообщений |
| node:20-alpine | Node.js приложения |
| python:3.11-slim | Python скрипты |
| ubuntu:22.04 | Общего назначения |

## Результат

Агент может **создавать и управлять реальным окружением** для тестирования приложений, баз данных и сервисов прямо из чата! 🎉

Это позволяет:
- ✅ Тестировать на реальных сервисах
- ✅ Создавать CI/CD окружения
- ✅ Проверять интеграции
- ✅ Отлаживать проблемы в контейнерах
- ✅ Демонстрировать работу на живых примерах
