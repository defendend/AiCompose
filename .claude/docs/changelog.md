---
name: changelog
description: История развития проекта AiCompose — хронология фич по дням
---

# Changelog

История развития проекта AiCompose.

---

## День 28: Оптимизация локальной LLM

Бенчмарк и оптимизация локальных моделей через Ollama.

**Добавлено:**
- CLI бенчмарк-скрипт `scripts/ollama_demo.sh` (6 тестов)
- Кастомная модель `Modelfile.chatbot` (qwen2.5:1.5b)
- Desktop UI "Бенчмарк Ollama" (🦙)
- Компоненты: `OllamaModels.kt`, `OllamaBenchmarkClient.kt`, `OllamaComparisonViewModel.kt`, `OllamaComparisonScreen.kt`
- Гайд `docs/OLLAMA_OPTIMIZATION_GUIDE.md`

**Результаты бенчмарка:**
| Модель | Конфигурация | Скорость |
|--------|--------------|----------|
| qwen2.5:1.5b | Сбалансированная (t=0.7) | **103 т/с** |
| qwen2.5:1.5b | Креативная (t=1.2) | 93 т/с |
| llama3.2:3b | Базовый | 5.5 т/с |

---

## День 22: Ассистент поддержки пользователей

Мини-сервис для поддержки пользователей с RAG для FAQ.

**Добавлено:**
- 7 support инструментов (get_ticket, search_tickets, get_user, create_ticket, update_ticket, get_faq, get_stats)
- `POST /api/support` endpoint
- `SupportRepository.kt` с JSON хранением
- `SupportScreen.kt` в Desktop приложении

**Тестовые данные:**
- 3 пользователя (BASIC, PRO, ENTERPRISE)
- 5 тикетов с разными статусами
- 8 FAQ записей по категориям

---

## День 21: Автоматизация Code Review

Система автоматического ревью Pull Request с интеграцией в GitHub Actions.

**Добавлено:**
- 4 GitHub инструмента (github_get_pr_info, github_get_pr_diff, github_get_pr_files, github_post_review)
- `POST /api/review` endpoint
- GitHub Action workflow `code-review.yml`

---

## День 19: RAG Chat Bot (CLI)

Интерактивный CLI чат-бот с RAG-памятью.

**Добавлено:**
- `RagChatBot.kt` — основной класс
- `RagChatDemo.kt` — CLI с цветным выводом
- Команды: /index, /history, /clear, /status, /help, /exit
- Скрипт `rag_chat_demo.sh`

---

## День 18: Реранкинг и фильтрация

Второй этап после поиска — фильтр релевантности.

**Добавлено:**
- `RerankerService.kt` с методами filterByRelevance, rerank, processResults
- Параметр `minRelevance` в DocumentIndex.search()
- Инструмент `compare_rag_with_reranking`
- Константы порогов: STRICT (0.5), MODERATE (0.3), RELAXED (0.1), NONE (0.0)

---

## День 17: Первый RAG-запрос

Полный pipeline RAG-запросов.

**Добавлено:**
- `RagQueryService.kt` с методами queryWithRag, queryWithoutRag, compareAnswers
- Инструменты `ask_with_rag`, `compare_rag_answers`
- Статистика: найдено фрагментов, источники, релевантность, время, токены

---

## День 16: RAG система

Базовая RAG система для векторного поиска.

**Добавлено:**
- `DocumentChunker.kt` — разбивка документов (500 символов, 50 overlap)
- `SimpleEmbeddings.kt` — TF-IDF векторизация
- `DocumentIndex.kt` — индекс документов (save/load JSON)
- 3 инструмента: rag_index_documents, rag_search, rag_index_info
- 4 интеграционных теста (всего 170)

---

## День 14: Уведомления

Система уведомлений о напоминаниях.

**Добавлено:**
- Инструмент `get_current_time` для относительного времени
- `ReminderScheduler` (проверка каждые 15 сек)
- `GET /api/reminders/notifications` endpoint
- In-app Snackbar в Desktop (polling каждые 30 сек)

---

## День 13: Планировщик + MCP

Reminder system с MCP интеграцией.

**Добавлено:**
- `ReminderRepository.kt` (JSON хранение)
- `ReminderScheduler.kt`
- 5 MCP инструментов для напоминаний
- Агент работает 24/7

---

## День 12: MCP интеграция

Model Context Protocol интеграция.

**Добавлено:**
- Weather MCP Server (Open-Meteo)
- `WeatherMcpClient.kt`
- `McpToolsAdapter.kt`
- UI экран MCP серверов

---

## Ранние версии

- Desktop UI с чатом и логами
- Backend с агентом и инструментами
- GitHub Actions для автодеплоя
- Агент "Профессор Архивариус" с историческими инструментами
- Режимы сбора данных (ТЗ, дизайн-бриф, резюме проекта, custom)
- Персонажи агента (смена System Prompt на лету)
- Shared модуль с общими моделями
- Декомпозиция Agent.kt (PromptBuilder, LLMClient, ConversationRepository)
- Dependency Injection (Koin 4.0.0)
- Streaming ответов (SSE)
- PostgreSQL и Redis хранилища
- Сжатие истории диалога

---

## Связанные документы

- Структура проекта — см. docs/structure.md
- Архитектура — см. rules/architecture.md
