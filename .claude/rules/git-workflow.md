---
name: git-workflow
description: Git workflow для проекта AiCompose — коммиты, ветки, PR
---

# Git Workflow

Правила работы с Git для проекта AiCompose.

## Коммиты

### Формат сообщения

```
<тип>: <краткое описание>

[опционально: детальное описание]

Co-Authored-By: Claude <noreply@anthropic.com>
```

### Типы коммитов

| Тип | Описание |
|-----|----------|
| `feat` | Новая функциональность |
| `fix` | Исправление бага |
| `refactor` | Рефакторинг без изменения поведения |
| `docs` | Документация |
| `test` | Добавление/изменение тестов |
| `chore` | Рутинные задачи (зависимости, конфиги) |
| `style` | Форматирование, стиль кода |

### Примеры

```
feat: добавить RAG систему для векторного поиска

- DocumentChunker для разбивки документов
- SimpleEmbeddings для TF-IDF
- DocumentIndex для хранения и поиска

Co-Authored-By: Claude <noreply@anthropic.com>
```

```
fix: исправить timeout при длинных запросах к DeepSeek

Увеличен таймаут с 60 до 120 секунд для backend.

Co-Authored-By: Claude <noreply@anthropic.com>
```

## Ветки

### Naming convention

```
feature/<название>     # Новая функциональность
fix/<название>         # Исправление бага
refactor/<название>    # Рефакторинг
docs/<название>        # Документация
```

### Примеры

```
feature/rag-system
fix/deepseek-timeout
refactor/agent-decomposition
docs/api-documentation
```

## Pull Requests

### Формат описания

```markdown
## Summary
- Краткое описание изменений (1-3 пункта)

## Test plan
- [ ] Что нужно проверить
- [ ] Какие тесты запустить

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

### Чеклист перед PR

- [ ] Код компилируется без ошибок
- [ ] Все тесты проходят (`./gradlew :backend:test`)
- [ ] Нет захардкоженных секретов
- [ ] Код соответствует style guide
- [ ] Добавлены тесты для новой функциональности

## CI/CD

### CI Workflow (ci.yml)
- Запускается при push и PR в `main`
- Сборка shared, backend, desktop
- Запуск тестов

### Deploy Workflow (deploy-backend.yml)
- Запускается при изменениях в `backend/**`, `shared/**`
- Сначала тесты, потом деплой

## Опасные операции

### Запрещено без явного запроса
- `git push --force`
- `git reset --hard`
- `git commit --amend` (на уже запушенных коммитах)
- Force push в main/master

### Требуют осторожности
- `git rebase` (только для локальных веток)
- `git cherry-pick`

---

## Связанные документы

- Деплой — см. docs/deployment.md
- Архитектура — см. rules/architecture.md
