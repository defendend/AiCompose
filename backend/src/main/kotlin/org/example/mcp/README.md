# MCP (Model Context Protocol) Integration

Интеграция Model Context Protocol в проект AiCompose.

## Что такое MCP?

[Model Context Protocol (MCP)](https://modelcontextprotocol.io) - это открытый протокол от Anthropic для стандартизации взаимодействия между LLM-приложениями и внешними источниками данных/инструментами.

## Установленная версия

- **MCP Kotlin SDK**: 0.8.1 (последняя на декабрь 2024)
- **Репозиторий**: [modelcontextprotocol/kotlin-sdk](https://github.com/modelcontextprotocol/kotlin-sdk)
- **Разработчики**: Anthropic + JetBrains

## Структура файлов

```
backend/src/main/kotlin/org/example/mcp/
├── README.md               # Эта документация
├── SimpleMcpServer.kt      # Базовый MCP сервер с STDIO транспортом
└── McpDemo.kt              # Демонстрация создания сервера и инструментов
```

## Запуск демонстрации

### Вариант 1: Gradle task (рекомендуется)

```bash
./gradlew :backend:runMcpDemo
```

**Вывод:**
```
======================================================================
🚀 MCP (Model Context Protocol) Демонстрация
======================================================================

✅ MCP сервер успешно создан!

📋 Зарегистрировано 5 инструментов:
   1. echo - Возвращает текст обратно
   2. add - Складывает два числа
   3. multiply - Умножает два числа
   4. get_time - Возвращает текущее время
   5. reverse - Переворачивает строку

======================================================================
✨ Демонстрация завершена!
```

### Вариант 2: Запуск MCP сервера через STDIO

SimpleMcpServer использует STDIO транспорт для взаимодействия (стандартный ввод/вывод). Это позволяет другим процессам подключаться к серверу через pipes.

```bash
./gradlew :backend:run -PmainClass=org.example.mcp.SimpleMcpServerKt
```

## Доступные инструменты (tools)

| Имя | Описание | Параметры |
|-----|----------|-----------|
| `echo` | Возвращает текст обратно | `text: string` |
| `add` | Складывает два числа | `a: integer, b: integer` |
| `multiply` | Умножает два числа | `a: integer, b: integer` |
| `get_time` | Возвращает текущее время ISO | нет |
| `reverse` | Переворачивает строку | `text: string` |

## Как работает код

### Создание сервера

```kotlin
val server = Server(
    serverInfo = Implementation(
        name = "simple-mcp-server",
        version = "1.0.0"
    ),
    options = ServerOptions(
        capabilities = ServerCapabilities(
            tools = ServerCapabilities.Tools(
                listChanged = false
            )
        )
    )
)
```

### Регистрация инструмента

```kotlin
server.addTool(
    name = "echo",
    description = "Возвращает переданный текст обратно"
) { request ->
    val args = request.arguments?.jsonObject
    val text = args?.get("text")?.jsonPrimitive?.content ?: ""

    CallToolResult(
        content = listOf(TextContent(text = "Echo: $text"))
    )
}
```

### Запуск сервера с STDIO транспортом

```kotlin
server.connect(
    StdioServerTransport(
        System.`in`.asSource().buffered(),
        System.out.asSink().buffered()
    )
)
```

## Поддерживаемые транспорты

MCP Kotlin SDK поддерживает несколько видов транспорта:

- **STDIO** - через стандартные потоки ввода/вывода (используется в SimpleMcpServer)
- **SSE (Server-Sent Events)** - для потоковой передачи через HTTP
- **WebSocket** - для двусторонней коммуникации
- **HTTP Streaming** - альтернативный HTTP transport

## Следующие шаги

Возможные направления развития MCP интеграции:

1. **Интеграция с основным агентом** - добавить MCP tools в существующий Agent.kt
2. **MCP клиент** - подключаться к внешним MCP серверам
3. **SSE/WebSocket транспорт** - для интеграции с веб-интерфейсом
4. **Расширение tools** - добавить специализированные инструменты для AiCompose

## Полезные ссылки

- [MCP Документация](https://modelcontextprotocol.io/docs)
- [MCP Kotlin SDK](https://modelcontextprotocol.github.io/kotlin-sdk/)
- [GitHub репозиторий](https://github.com/modelcontextprotocol/kotlin-sdk)
- [MCP Specification](https://spec.modelcontextprotocol.io/)

## Примечания

- SDK версии 0.8.1 содержит deprecated методы - планируется миграция в будущих версиях
- STDIO транспорт требует запуска сервера как отдельного процесса
- Для продакшн использования рекомендуется использовать SSE или WebSocket транспорты
